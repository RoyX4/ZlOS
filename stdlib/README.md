# Standard Library

Reusable zl modules live here. Example programs belong in `examples/`; focused
regression programs belong in `tests/`.

This directory is currently flat because the import system and existing callers
use these module paths directly. Proposed package splits in design documents are
not implemented merely because this README exists.
