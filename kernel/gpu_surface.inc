/* gpu_surface.inc - GENERATED by gen_gpu_surface.py. Do not edit by hand.
 *
 * Gen9 RENDER_SURFACE_STATE field positions, lifted out of Mesa's own
 * decoder database - which ships ZLIB-COMPRESSED inside libgallium, which is
 * why twelve INTEL_DEBUG flags and four filesystem searches never found it.
 * Source: /usr/lib/x86_64-linux-gnu/libgallium-26.1.5-1.so
 *
 * The structure is 16 dwords. Only the fields a render target sets are
 * emitted; everything else is zero, which is the correct default for a
 * linear 2D colour target with no auxiliary surface.
 *
 * Each field is a dword index, a shift and a mask, so a builder writes
 *     dw[GPU_RSS_WIDTH_DW] |= (v & GPU_RSS_WIDTH_MASK) << GPU_RSS_WIDTH_SHIFT;
 * and cannot silently put a value in the wrong dword.
 */
#define GPU_RSS_DWORDS 16u

/* Tile Mode  dw0 bits 13:12 */
#define GPU_RSS_TILE_MODE_DW    0u
#define GPU_RSS_TILE_MODE_SHIFT 12u
#define GPU_RSS_TILE_MODE_MASK  0x3u

/* Surface Horizontal Alignment  dw0 bits 15:14 */
#define GPU_RSS_SURFACE_HORIZONTAL_ALIGNMENT_DW    0u
#define GPU_RSS_SURFACE_HORIZONTAL_ALIGNMENT_SHIFT 14u
#define GPU_RSS_SURFACE_HORIZONTAL_ALIGNMENT_MASK  0x3u

/* Surface Vertical Alignment  dw0 bits 17:16 */
#define GPU_RSS_SURFACE_VERTICAL_ALIGNMENT_DW    0u
#define GPU_RSS_SURFACE_VERTICAL_ALIGNMENT_SHIFT 16u
#define GPU_RSS_SURFACE_VERTICAL_ALIGNMENT_MASK  0x3u

/* Surface Format  dw0 bits 27:18 */
#define GPU_RSS_SURFACE_FORMAT_DW    0u
#define GPU_RSS_SURFACE_FORMAT_SHIFT 18u
#define GPU_RSS_SURFACE_FORMAT_MASK  0x3FFu

/* Surface Type  dw0 bits 31:29 */
#define GPU_RSS_SURFACE_TYPE_DW    0u
#define GPU_RSS_SURFACE_TYPE_SHIFT 29u
#define GPU_RSS_SURFACE_TYPE_MASK  0x7u

/* Width  dw2 bits 13:0 */
#define GPU_RSS_WIDTH_DW    2u
#define GPU_RSS_WIDTH_SHIFT 0u
#define GPU_RSS_WIDTH_MASK  0x3FFFu

/* Height  dw2 bits 29:16 */
#define GPU_RSS_HEIGHT_DW    2u
#define GPU_RSS_HEIGHT_SHIFT 16u
#define GPU_RSS_HEIGHT_MASK  0x3FFFu

/* Surface Pitch  dw3 bits 17:0 */
#define GPU_RSS_SURFACE_PITCH_DW    3u
#define GPU_RSS_SURFACE_PITCH_SHIFT 0u
#define GPU_RSS_SURFACE_PITCH_MASK  0x3FFFFu

/* Depth  dw3 bits 31:21 */
#define GPU_RSS_DEPTH_DW    3u
#define GPU_RSS_DEPTH_SHIFT 21u
#define GPU_RSS_DEPTH_MASK  0x7FFu

/* Render Target View Extent  dw4 bits 17:7 */
#define GPU_RSS_RENDER_TARGET_VIEW_EXTENT_DW    4u
#define GPU_RSS_RENDER_TARGET_VIEW_EXTENT_SHIFT 7u
#define GPU_RSS_RENDER_TARGET_VIEW_EXTENT_MASK  0x7FFu

/* Surface Base Address: 64 bits from dw8 - spans two dwords, write low then high */
#define GPU_RSS_SURFACE_BASE_ADDRESS_DW    8u
#define GPU_RSS_SURFACE_BASE_ADDRESS_SHIFT 0u
#define GPU_RSS_SURFACE_BASE_ADDRESS_IS64  1

/* the enum values a linear 2D colour target uses */
#define GPU_RSS_SURFTYPE_2D 1u
#define GPU_RSS_LINEAR 0u
#define GPU_RSS_XMAJOR 2u
#define GPU_RSS_YMAJOR 3u
