# zlOS Tests

- `host/`: native subsystem harnesses using shipping kernel source
- `oracle/`: QEMU captures and visual comparison tooling
- `refrender/`: independent browser-rendered design reference
- `fixtures/`: stable inputs such as the serial golden transcript

Generated binaries and screenshots are ignored; retained evidence belongs in
`../docs/evidence/`.
