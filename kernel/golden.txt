  zlOS starting

  [  OK  ] multiboot handoff, 32-bit protected mode
  [  OK  ] stack established, 16 KiB
  [  OK  ] COM1 initialised, 115200 8N1
  [  OK  ] VGA text console, 80x25
  [  OK  ] GDT loaded - flat 4 GiB code and data segments
  [  OK  ] IDT installed, PIC remapped, interrupts ON
  [  OK  ] PIT timer running at 100 Hz on IRQ0
  [  OK  ] keyboard on IRQ1 - no more polling the port
  [  OK  ] zl runtime, kernel subset
  [ INFO ] no heap, no filesystem, no scheduler

  ready.
  press h for help, q to halt

  zl> 
  commands
    h        this help
    <n> f    fib(n)          e.g.  20f
    <n> s    sum of squares  e.g.  10s
    t        uptime, from the timer interrupt
    b        colour-bar graphics demo
    m        poke a byte into video memory and read it back
    c        clear the screen
    q        halt the machine
  zl> 20
  6765
  zl> 10
  385
  zl> 
  wrote a byte to video memory, read back: 42
  zl> 

  halting.

[kernel] main() returned - halting
