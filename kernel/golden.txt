  settings: no NVMe, load skipped
  zlOS starting

  [  OK  ] multiboot handoff, 32-bit protected mode
  [  OK  ] stack established, 256 KiB
  [  OK  ] COM1 initialised, 115200 8N1
  [  OK  ] VGA text console, 80x25
  [  OK  ] GDT loaded - flat 4 GiB code and data segments
  [  OK  ] IDT installed, PIC remapped, interrupts ON
  [  OK  ] APIC: IRQs via I/O APIC at 0xFEC00000, 1 CPU(s)
  [  OK  ] PIT timer running at 100 Hz on IRQ0
  [  OK  ] keyboard on IRQ1 - no more polling the port
  USB: no xHCI controller (6 PCI devices seen)
  arena: 16 MiB at 14 MiB, ends at 30 MiB, ceiling 128 MiB (bg_buf)
  heap: 64 MiB at 256 MiB, ends at 320 MiB
  vmm: paging is off on this build - identity, by construction
  ring 3: u31 <- from ring 3 via int 0x80, 5 syscalls, returned to ring 0
  [  OK  ] zl runtime, kernel subset
  [ INFO ] no heap, zlfs mounts on demand, no scheduler
  app-manifest: schema=1 entries=62 sha256=a1c4ec15e88d709e59096dce8f08b1b6e37adee91148d706c439f59d3d2d9da7
  build-identity: schema=1 id=85027b159c9a594045c2f900e5971bb3408dd418dd61a373625425fba9030d13
  build-source: head=b8a00ec45ac1f9f955ba79ed63f0072540067d4e dirty=1

  ready.
  press h for help, q to halt

  zl> 
  commands
    h        this help
    <n> f    fib(n)          e.g.  20f
    <n> s    sum of squares  e.g.  10s
    t        uptime, from the timer interrupt
    p        the CPU's own brand string, via CPUID
    b        colour-bar graphics demo (with sound)
    a        animation - a bouncing logo over a starfield
    x        mouse demo - a cursor driven by the PS/2 mouse
    g        play SNAKE (w a s d to steer)
    d        paint with the mouse
    <n> i    edit legacy RAM slot n (0-9)
    l        Files app (graphical mode) / RAM slots (text mode)
    run F    run a program from a file (type it - needs a name)
    .        zlfs on the NVMe disk - mount, and count the boots
    ,        list the files ON DISK, by name
    ;        the real time, read off the CMOS clock
    '        write the clipboard to clip.txt on disk
             (Ctrl+C in the editor copies, Ctrl+V pastes)
    e        beep the PC speaker
    m        poke a byte into video memory and read it back
  our hardware drivers
    k        PCI bus + the Intel GPU, read off real silicon
    u        USB: take the xHCI controller and enumerate the bus
    j        USB keyboard - live, straight off the HID reports
    n        change the screen resolution, no BIOS involved
    v        spinning 3D cube, rendered in software
    w        draggable windows
    y        virtio-gpu - a real GPU driver, virtqueues and all
    z        the processor: CPUID, topology, caches, TSC clock
    o        NVMe disk - identify, then write and read a block back
    +        multitasking - three tasks running beside the shell
    *        SMP - wake the other CPU cores with INIT/STARTUP IPIs
    /        USB mass storage - SCSI over USB, read a sector
    ?        I2C-HID touchpad (real hardware only - QEMU has no LPSS I2C)
    =        input events - keys, modifiers, repeat, live
    c        clear the screen
    r        reboot the machine
    q        halt the machine
  zl> 20
  6765
  zl> 10
  385
  zl> 
  wrote a byte to video memory, read back: 42
  zl> 

  halting.

[kernel] main() returned - halting
