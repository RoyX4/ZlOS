# zlOS Apps

Application and game modules written in zl live here. `apps_registry.zl` owns
the catalog and imports the slices; `apps_common.zl` contains shared app code.

App IDs are runtime routing identifiers. Run
`../tools/checks/check-appids.py --selftest` after adding or changing one.
