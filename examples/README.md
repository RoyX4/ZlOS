# Examples

Runnable zl programs live here. They demonstrate complete user-facing programs;
small assertions and regressions belong in `tests/`, while reusable modules
belong in `stdlib/`.

Run one from the repository root with `./interp examples/hello.zl`. Generated C,
LLVM IR, executables, and screenshots do not belong in this directory.
