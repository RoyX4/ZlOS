{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build toolchain",
            "type": "shell",
            "command": "./build.sh",
            "group": { "kind": "build", "isDefault": true },
            "problemMatcher": ["$gcc"],
            "detail": "Builds interp, compile, compilel, nativegen and the demo binaries"
        },
        {
            "label": "run tests",
            "type": "shell",
            "command": "./run_tests.sh",
            "group": { "kind": "test", "isDefault": true },
            "problemMatcher": [],
            "detail": "Full suite: interpreter + cross-checks against all three compiled backends"
        },
        {
            "label": "run current .zl file (interpreter)",
            "type": "shell",
            "command": "./interp",
            "args": [{ "value": "${file}", "quoting": "strong" }],
            "problemMatcher": [],
            "detail": "Runs the open .zl file through the tree-walking interpreter"
        },
        {
            "label": "compile current .zl file (C backend)",
            "type": "shell",
            "command": "./compile '${file}' && gcc -O2 -D_strdup=strdup -Isrc/runtime -o '${fileBasenameNoExtension}' out.c src/runtime/runtime.c src/runtime/os_linux.c -lm && echo 'built ./${fileBasenameNoExtension}'",
            "problemMatcher": ["$gcc"],
            "detail": "zl -> C -> gcc -> native ELF binary"
        },
        {
            "label": "compile current .zl file (LLVM backend)",
            "type": "shell",
            "command": "./compilel '${file}' && clang -O2 out.ll -o '${fileBasenameNoExtension}' && echo 'built ./${fileBasenameNoExtension}'",
            "problemMatcher": [],
            "detail": "zl -> LLVM IR -> clang -> native ELF binary (unboxed subset)"
        },
        {
            "label": "compile current .zl file (native x86-64, no C compiler)",
            "type": "shell",
            "command": "./nativegen '${file}' && echo 'built ./native_out'",
            "problemMatcher": [],
            "detail": "zl -> hand-assembled machine code -> static ELF (integer subset only)"
        }
    ]
}
