# Build specs for the shell surfaces, extracted from the prototype

2026-08-29. Eleven agents read `docs/design/presswork-prototype.html` and
produced implementation specs; a twelfth re-derived three entries from each
independently.

**HOW TO USE THESE.** Every one of the eleven spot-checks came back `ok=false`,
and the pattern is consistent: the checkers found the DECLARED values (the CSS
a spec quotes) exact, and the DERIVED values (heights summed from padding +
line-height + borders) wrong. So the quoted tokens, label text and declarations
here are reliable; every computed number is a claim to re-derive before use.
That is the same rule this repo applies to its own figures.

`MISSING` in the zlOS column means the element has no counterpart today.

## palette — 24 elements, 5 missing

Drawn today by: /home/roy/Documents/repos/zl-linux-presswork/kernel/src/kernel.zl:9666 `fn ov_draw_palette(u)` (body 9666-9711). Supporting: OV_PALETTE=1 (:9605), ov_sel (:9610), OV_SCRIM_NUM/DEN=5/8 (:9621-9622), OV_SHEET_W=520 / OV_SHEET_MAXH=420 (:9627-9628), ov_sheet_x/ov_sheet_y (:9629-9630), OV_PAL_N=10 (:963

| # | element | exact text | geometry | tokens | zlOS today | backing |
|---|---|---|---|---|---|---|
| 1 | #palette root — the modal layer itself |  | position:absolute; inset:0; z-index:260; display:none, display:block when .on. I | none — no paint of its own | kernel.zl:9605 OV_PALETTE=1 as a mode on the single ov_mode  | N/A |
| 2 | .scrim — the shade over everything |  | position:absolute; inset:0 (full #os, i.e. everything below the prototype's own  | DARK (:889): background: color-mix(in srgb, var(--zd-cut) 78 | kernel.zl:10092 shade(x, y, w, h, OV_SCRIM_NUM=5, OV_SCRIM_D | N/A |
| 3 | .sheet — the plate the whole palette sits on |  | position:absolute; left:50%; transform:translateX(-50%) — centred on the FULL #o | ground --zd-float (#4B4440 dark / #FDF8EE light) — the off-t | kernel.zl:9667-9675. sx = ov_sheet_x() = (px_w() - 520*u)/2  | READ |
| 4 | .ph — the header band |  | display:flex; align-items:center; gap: calc(10px * var(--ui)); padding: 0 calc(1 | ground --zd-knock (#B6B0AB dark / #47433D light) — the band  | kernel.zl:9677-9683 draws the header CONTENT but paints NO G | N/A |
| 5 | .ph — the word COMMAND | command | First flex child. x = sheet left + 1 (border) + 11 (padding) = 12 px inside the  | ink --zd-knock-ink (#181411 dark, stated 8.53:1 on the knock | kernel.zl:9678 ui_caps(sx + 13*u, sy + 9*u, "COMMAND", theme | N/A |
| 6 | .ph — the qualifier zlsh builtin | zlsh builtin | Second flex child, 10 px gap after COMMAND. Class .t-num: font-family var(--mono | ink --zd-knock-ink2 (#46413D dark, stated 4.70:1 / #B6B1A7 l | kernel.zl:9679 label(sx + 92*u, sy + 9*u, "zlsh builtin", th | CANNOT |
| 7 | #pcount — the N of M readout | e.g. "4 of 16" — built by renderPalette (:2587) as: m.length + ' of '  | Third flex child, carries .spread (margin-left:auto, :367) so it is pushed hard  | ink --zd-knock-ink2, same rule at :902. | kernel.zl:9680-9682 draws it as FOUR separate calls because  | BACKABLE |
| 8 | .pin — the input band |  | display:flex; align-items:center; gap: calc(8px * var(--ui)); padding: calc(6px  | ground --zd-well (#17100C dark / #C2BDB3 light) — a SUNKEN s | kernel.zl:9684-9689. The band runs sy+26*u to sy+54*u — 28 u | N/A |
| 9 | .pin — the word RUN | run | First child of .pin, 12 px in from the sheet's left edge (1 border + 11 padding) | ink --zd-text-3 (#B9B3AA dark, stated 6.68:1 on BASE / 4.59: | kernel.zl:9685 ui_caps(sx + 13*u, sy + 34*u, "RUN", theme(TH | N/A |
| 10 | #pq — the echo of what has been typed | variable: the literal contents of S.pq, rendered as-is with no transfo | Second child of .pin, 8 px gap after RUN. Mono, 11px/15px, tabular-nums. It has  | ink --zd-text-0 via .ink0 (:356) — #ECE6DD dark (stated 11.2 | MISSING ENTIRELY. kernel.zl:9686-9688's own comment states i | BACKABLE |
| 11 | .caret — the insertion mark |  | display:inline-block; width: calc(7px * var(--ui)); height: calc(13px * var(--ui | --zd-verm (#E8734F dark, stated 4.6319:1 on BASE / #B7311A l | kernel.zl:9687 fill_rgb(sx + 52*u, sy + 33*u, 2*u, cell_h(), | READ |
| 12 | pmatch() — the filter predicate, and the one hard blocker on |  | No geometry. Semantics, verbatim from :2579-2582: q = S.pq.toLowerCase(); return |  | Does not exist. OV_PAL_N rows are drawn unconditionally by t | BACKABLE |
| 13 | #presults — the row viewport |  | max-height: calc(300px * var(--ui)); overflow-y: auto. No padding, no background | none of its own — the sheet's --zd-float shows through betwe | No viewport concept. kernel.zl:9690-9701 draws OV_PAL_N rows | BACKABLE |
| 14 | the scrollbar, when #presults overflows |  | Webkit scrollbar rules at :332-334: width and height calc(9px * var(--ui)); trac | track --zd-well; thumb --zd-text-inert (#6E685F dark / #8883 | MISSING. TH_SURF_7 is role 26, assigned ZD_TEXT_INERT at ui. | BACKABLE |
| 15 | the 16 CMDS rows — all of them, verbatim, in order | CMDS (:1520-1537), as [label, kind, target]. Row 1  'open 03 monitor'  | Draw order is array order, top to bottom, no sorting — pmatch preserves it via A |  | kernel.zl:9635-9656 carries TEN rows, not sixteen: OV_PAL_N  | N/A |
| 16 | .prow — one result row, unselected |  | display:flex; align-items:center; gap: calc(10px * var(--ui)); position:relative | separator --zd-cut. ground inherited --zd-float. | kernel.zl:9690-9701. rowh = 22*u (:9670) against the prototy | N/A |
| 17 | .prow contents — the three cells, in flex order | Cell 1, the ordinal: renderPalette (:2591) builds it as ('0' + (i + 1) | Ordinal at 12 px from the sheet's left edge (1 border + 11 padding); mono 11px/1 | ordinal --zd-text-3 via .ink3 (#B9B3AA dark / #56514D light) | kernel.zl:9697-9699. Ordinal: label_num(sx + 13*u, ry + 4*u, | READ |
| 18 | .prow.on — the selected row |  | Same 27 px box. The row's ground goes full-bleed, edge to edge of the sheet — no | ground --zd-knock (#B6B0AB dark / #47433D light) — A KNOCKOU | kernel.zl:9692-9696: fill_rgb(sx, ry, sw, rowh, theme(TH_PAN | N/A |
| 19 | .prow:hover — the pointer response |  | Same box; ground only. | background --zd-raise (#413A36 dark / #F4EFE5 light), and it | MISSING. There is no pointer path into the overlay at all —  | N/A |
| 20 | the empty state | no match | Emitted by renderPalette's `\|\| ` fallback (:2594) when the filtered list is em | ink --zd-text-3 via .ink3; ground inherited --zd-float. | MISSING — unreachable today, because nothing filters. It bec | N/A |
| 21 | .pf — the footer band |  | display:flex; align-items:center; gap: calc(10px * var(--ui)); padding: calc(6px | top run --zd-lit (#6F6864 dark, stated 2.5423:1 on ZD_BASE / | kernel.zl:9702-9704: fy = sy + 62*u + OV_PAL_N*rowh + 6*u, t | N/A |
| 22 | .pf — the three key hints | Six spans, strictly alternating .t-lab then .t-num .ink3, source text  | All six are flex children with the same 10 px gap — so the gap between a key and | keys --zd-text-3 (.t-lab default). verbs --zd-text-3 (.ink3) | kernel.zl:9705-9710, six calls, all text verbatim correct: u | READ |
| 23 | the keydown contract, verbatim |  | From the palette block at :2957-2966, which is checked BEFORE the lock block and |  | Almost entirely absent. desk_key (kernel.zl:10326) answers E | BACKABLE |
| 24 | click-to-dismiss on the scrim, and click-to-run on a row |  | Scrim: `if (t.closest('#palette .scrim')) showPalette(false)` (:2855) — it is th |  | MISSING. No pointer routing into the overlay exists — wm.c h | BACKABLE |

**Build order.** Build in this order, because each step is a precondition of the next and each one is separately visible on screen.

1. GEOMETRY FIRST (elements 1-4, 9, 13, 21). Widen OV_SHEET_W 520 -> 580, move the sheet ground from TH_PANEL to TH_SURF_5, add the rrect radius and the ring, change rowh 22 -> 27, and re-derive the band offsets from the measured stack (28/28/27/29) instead of the current magic 26/54/62/6. This is the whole visual difference and it needs no new native and no new data. Do it before touching the list, or every later change lands on a plate that is still the wrong size and the wrong colour.

2. THE LIST (element 15). Raise OV_PAL_N 10 -> 16 and add the six missing rows to ov_pal_label / ov_pal_kind in the prototype's exact order. Pure data, no new machinery, and it makes the count in step 4 mean something.

3. THE SELECTED ROW (element 18). Swap TH_PANEL_HI+TH_ACCENT for TH_KNOCK + TH_KO_EDGE mark + TH_KNOCK_INK on all three cells. Small, self-contained, and it is a correctness fix the prototype writes a nine-line comment about (:914-923) — the accent bar computes 1.40:1 on the knockout and disappears.

4. RIGHT-EDGE COLUMNS BY MEASUREMENT (elements 7, 17). Replace sw - 76*u, sw - 52*u, sw - 34*u, sw - 20*u with ui_capsw()/ui_tnw() reads. Do it here and not earlier: once the sheet is 580 wide and the rows number 16, the hardcoded offsets are visibly wrong and the fix is obvious to check.

5. INTERACTION, IN ONE INCREMENT (elements 10, 11, 19, 20, 23, 24). PAL_BUF v

**Spot-check disagreed:** Entries 1 and 2 are correct on every declared value. Entry 3 is correct on every DECLARED value but its one DERIVED number is wrong, and the zlOS port line it hands you is wrong as a consequence.  === 1. #palette root — EXACT === :878  `#palette { position: absolute; inset: 0; z-index: 260; display: none; }` :879  `#palette.on { display: block; }` Child of #os, not #field: confirmed structurally, not just by the comment. `<div id="os">` :983, `<div id="rail">` :984, `<div id="fieldwrap">` :1006, `<div id="raster">` :1007, `<div id="field">` :1013; `<div id="palette">` :1044 at the same 2-space indent as #rail/#fieldwrap, closing `</div>` :1063, #os closes :1067. So #palette is a direct child of #os. Comment cited at :1038-1043 — correct, :1038 is `<!-- THE ONE MODAL, and it is modal over t

## activities — 19 elements, 2 missing

Drawn today by: kernel/src/kernel.zl:9929 `fn ov_draw_activities(u)`, reached from kernel/src/kernel.zl:10096 in `overlay_draw`. Helpers: `ov_act_n` kernel/src/kernel.zl:9905, `ov_act_win` kernel/src/kernel.zl:9915. Constants: OV_ACT_COLS=3 :9897, OV_ACT_GAP=14 :9898, OV_ACT_TH=20 :9899, OV_ACT_PAD=28 :9900, OV_ACT

| # | element | exact text | geometry | tokens | zlOS today | backing |
|---|---|---|---|---|---|---|
| 1 | THE SURFACE. An opaque flat panel over the desk only — not a |  | Rect = field_x(), field_y(), field_w(), field_h() exactly (kernel.zl:443-446). F | ground --zd-void = TH_BG (role 0, ui_color index 0, = ZD_VOI | WRONG SURFACE TYPE. overlay_draw (kernel.zl:10077-10093) sha | N/A |
| 2 | THE .ohead BAND and its 2dp struck rule. A header row across |  | Full surface width. Total height 38dp = 9dp padding-top + 18dp content (--lh-md) | band has no fill of its own (ZD_VOID shows through). Rule =  | OV_ACT_HEAD = 34 (kernel.zl:9901) — 4dp short of the real 38 | N/A |
| 3 | THE TITLE RUN. First item in .ohead. | activities  — the source text is lowercase 'activities' (prototype:101 | x = field_x() + 14*u. Size is MD 13dp / line-height 18dp — NOT LG. The span carr | ink = --zd-text-0 (t-big's own colour, prototype:350-351) =  | kernel.zl:9936 — `ui_caps(gx0, strip_h() + OV_ACT_PAD * u, " | N/A |
| 4 | #ovsub — the subtitle run, second item in .ohead. The one pl | Concatenated at prototype:2370-2371 as: on + ' plates on workspace 0'  | x = right edge of the ACTIVITIES run + 12*u (the .ohead gap). Face is MONO — .t- | ink = .ink3 = --zd-text-3 = TH_TEXT_DIM (role 4, ZD_TEXT_3 # | kernel.zl:9937-9938 — `label_num(gx0 + 108 * u, ..., n, them | BACKABLE |
| 5 | THE DISMISS HINT — third item in .ohead, pushed to the right | esc to dismiss  — LOWERCASE, verbatim. It is .t-num, which applies NO  | Right edge = field_x() + field_w() - 14*u. x = that minus the run's measured wid | ink = .ink3 = --zd-text-3 = TH_TEXT_DIM (role 4). | kernel.zl:9939 — `ui_caps(px_w() - OV_ACT_PAD * u - 96 * u,  | N/A |
| 6 | THE .ogrid RECT — the area the tiles are laid out in. It is  |  | gx0 = field_x() + 14*u gy0 = field_y() + 38*u + 14*u gw  = field_w() - 28*u gh   | none — layout rect, not a surface. | kernel.zl:9931-9934 — gx0 = rail_w() + 28*u; gy0 = strip_h() | N/A |
| 7 | THE TILE LIST AND ITS ORDER. Which windows get a tile, and i |  | Set = windows that are OPEN, on the CURRENT workspace, and NOT MINIMIZED (`.filt | none. | BACKWARDS AND UNFILTERED. ov_act_n (kernel.zl:9905-9913) and | BACKABLE |
| 8 | TILE GEOMETRY AND THE ONE SCALE. The arithmetic that turns t |  | cols = min(3, max(1, n))                     [OV_ACT_COLS = 3] rows = ceil(n / c | none. | kernel.zl:9942-9975. Two real faults. (1) `ty = gy0 + r * (c | READ |
| 9 | TILE GROUND AND RADIUS. |  | Rounded rect at (left, top, tw, th) from element 8. Radius = var(--zd-r-inset) = | ground = --zd-base = TH_PANEL (role 1, ZD_BASE #322B27 graph | kernel.zl:9977 — `fill_rgb(tx, ty, tw, th, theme(TH_PANEL))` | N/A |
| 10 | TILE BORDER. 1px, all four sides, ZD_EDGE_OVER, on EVERY til |  | 1px (not 1*u — the prototype writes `border: 1px solid`, an unscaled hairline, p | --zd-edge-over = TH_EDGE_OVER (role 33, ZD_EDGE_OVER #A09A95 | NO BORDER. Instead kernel.zl:9978-9979 draw a 1px ZD_LIT run | N/A |
| 11 | THE .th HEADER BAND on each tile, unfocused state, plus its  |  | Height 20*u — the same value as --zd-band-h (prototype:169) and BAND_H_D = 20 (k | unfocused: NO fill — ZD_BASE shows through from element 9. G | Height is right (OV_ACT_TH = 20, kernel.zl:9899) and the gro | N/A |
| 12 | TILE HEADER RUN 1 — the register number. | Two digits, zero-padded: 01 02 03 04 05 06 07 08 09 10 11 12 13 14 (th | x = tx + 1 + 7*u (border + .th padding-left). MONO (.t-num), SM 11dp / 15dp, tab | ink = .ink3 = --zd-text-3 = TH_TEXT_DIM (role 4). Flips to - | kernel.zl:9986 — `label_num(tx + 8 * u, ty + 4 * u, osl + 1, | READ |
| 13 | TILE HEADER RUN 2 — the application name. | The APPS `name` field, source lowercase, rendered UPPERCASE by .t-lab' | x = (register run's right edge) + 6*u — measure the register run, do not guess.  | ink = the inline override `color:var(--zd-text-2)` (prototyp | kernel.zl:9987 — `label(tx + 28 * u, ty + 4 * u, rail_name(o | READ |
| 14 | TILE HEADER RUN 3 — the window's real size, right-aligned by | w.w + ' x ' + w.h  (prototype:2555). Renders e.g.  640 x 400  — a LOWE | Right edge = tx + tw - 1 - 7*u. x = that minus the measured width of the WHOLE r | ink = .ink3 = --zd-text-3 = TH_TEXT_DIM (role 4). Flips to T | kernel.zl:9988-9990 — three calls at FIXED offsets from the  | READ |
| 15 | THE FOCUSED TILE'S KNOCKOUT. The header band inverts. This i |  | The same 20*u band from element 11, now FILLED edge to edge inside the 1px borde | fill = --zd-knock = TH_KNOCK (role 34, ZD_KNOCK #B6B0AB grap | MISSING ENTIRELY. kernel.zl:9981-9983 draw only the vermilio | READ |
| 16 | THE FOCUSED TILE'S INK FLIP. All three header runs reverse o |  | No geometry change — same three runs, same positions, same faces and tracks as e | ALL THREE go to --zd-knock-ink = TH_KNOCK_INK (role 35, ZD_K | MISSING — follows from element 15 being missing. The three l | READ |
| 17 | THE .tc PREVIEW — the window's own client surface, scaled, f |  | Rect = x tx+1, y ty + 20*u, width tw-2, height th - 20*u - 1 (the padding box be | no ink of its own. A window that has never painted leaves BA | kernel.zl:9995 — `wm_thumb(ow2, tx, ty + OV_ACT_TH * u, tw,  | READ |
| 18 | THE VERMILION FOCUS BAR — .thumb.on::after. The second and l |  | x = tx (the tile's own left edge, left:0 relative to the padding box, so tx+1 on | --zd-verm = TH_ACCENT (role 5, ZD_VERM #E8734F graphite / #B | WRONG START, WRONG PAINT ORDER, AND THE RESULT INVERTS ITSEL | READ |
| 19 | THE EMPTY STATE — zero windows on the workspace. | There is none. No 'no plates' copy, no placeholder, no illustration. T | The .ogrid rect is simply empty ZD_VOID. The prototype's guards (`Math.max(1, li | ground --zd-void = TH_BG (role 0). | kernel.zl:9941 — `if n < 1 { return 0 }`, placed AFTER the f | N/A |

**Build order.** Build in this order. Each step is independently verifiable on screen, and the ones that gate others come first.

1. ELEMENT 1 — the surface type. Branch in overlay_draw (kernel.zl:10077-10093) so OV_ACTIVITIES gets an opaque fill_rgb over field_x()/field_y()/field_w()/field_h() instead of the whole-screen 5/8 shade. FIRST because until the desk is opaque you cannot judge any tile's contrast against it, and every value on this surface was priced against ZD_VOID, not against a dimmed window. One `if` on an integer mode — no string comparison, no new native.

2. ELEMENTS 2 AND 6 — re-anchor the surface on field_x()/field_y()/field_w()/field_h() (kernel.zl:443-446), fix OV_ACT_PAD 28 -> 14 (kernel.zl:9900), OV_ACT_HEAD 34 -> 38 (kernel.zl:9901), and draw the 2dp TH_LIT rule under the header. SECOND because every subsequent x and y is measured from these two rects; fixing tiles first means measuring them twice.

3. ELEMENT 7 — reverse the walk in ov_act_n / ov_act_win (kernel.zl:9905-9926) to run wm_n()-1 down to 0. Do this before touching tile geometry: with the order wrong you will be looking at the wrong tile while you tune the layout, and the focused tile — the one carrying all the new treatment — is currently the LAST tile drawn rather than the first.

4. THE ONE BINDING — add wm_min to runtime_kernel.c beside wm_winws at :1744, exposing wm_is_minimized (wm.c:1531). This is the only C change in the whole spec. Do it here so elements 4 and 7 can both take the filter in the sam

**Spot-check disagreed:** ENTRY 3 IS WRONG (one clause). Entries 1 and 2 are exact on every value.  === ENTRY 1 — THE SURFACE: EXACT === prototype:851-852 "#overview { position: absolute; inset: 0; z-index: 200; display: none;\n            background: var(--zd-void); }" prototype:853 "#overview.on { display: flex; flex-direction: column; }" Opaque flat --zd-void, no border, no gradient, z-index 200. Confirmed inside #field (markup 1016-1025), so field-only, not screen-wide — and that is deliberate: the prototype's own comment at 1037-1042 says the PALETTE was moved out of #field because "its scrim stopped at the desk ... reads as a clipping fault"; #overview was left in. Covers, all inside #field and all below z 200: #field::before wallpaper glow z 0 (prototype:531), #gridsvg z 1 (prototype:541), #gridleg z 1 (prot

## menus — 35 elements, 3 missing

Drawn today by: kernel/src/kernel.zl:9830 `ov_draw_menu(u)` (body 9830-9883). Row tables: kernel.zl:9730-9772 (`OV_CTX_N`, `ov_ctx_label/sep/haskey/key`), kernel.zl:9774-9802 (`OV_WIN_N`, `ov_win_label/sep/haskey/key/dis`). Dispatchers kernel.zl:9804-9828. Width const `OV_MENU_W = 210` at kernel.zl:9804. Openers ke

| # | element | exact text | geometry | tokens | zlOS today | backing |
|---|---|---|---|---|---|---|
| 1 | THE MENU BOX (shared by both menus). `.menu` — the container |  | min-width 214 (a FLOOR, not a fixed width — rows are flex and the box grows to t | ground `--zd-float` #4B4440 = `theme(TH_SURF_5)` (ui.c:186,  | WRONG GROUND, NO RING, NO RADIUS, NO SHADOW. kernel.zl:9853  | N/A |
| 2 | PLACEMENT AND EDGE CLAMP (shared). Where the box lands relat |  | left = min(clientX - #os.left, #os.width - menuWidth - 8); top = min(clientY - # | none — geometry only. | CORRECT, AND BETTER. kernel.zl:9843-9852 implements exactly  | READ |
| 3 | THE HEADER BAND `.menu .mh` (shared shell, different text pe | see elements 11 and 24 for the two literal strings | height 24, full bleed to the box's inner edges (the 4px box radius clips its top | band fill `--zd-knock` #B6B0AB = `theme(TH_KNOCK)` (TH id 34 | MISSING — THERE IS NO BAND. kernel.zl:9857-9862 draws only ` | N/A |
| 4 | THE ROW BOX `.mi` (shared by every non-separator row in both |  | padding 3 top, 11 right, 3 bottom, 11 left. `display:flex; align-items:center; g | text `--zd-text-1` #DAD4CB = `theme(TH_TEXT)` (ui.c:156, `th | kernel.zl:9871 `rowh = 20 * u` and kernel.zl:9884 `label(mx  | N/A |
| 5 | THE LABEL SPAN (left column of every row). `<span class="t-m | row-dependent; see elements 12-23 and 25-33 | font-size 13, line-height 18, weight 400 (regular), sentence case as written — N | colour inherited from `.mi` = `--zd-text-1` / `theme(TH_TEXT | kernel.zl:9884 `label(mx + 14*u, ry + 3*u, ov_menu_label(mi) | N/A |
| 6 | THE VALUE / KEY COLUMN (right column). `<span class="spread  | empty string when the row has none — the span is still emitted, it jus | `margin-left:auto` right-flushes it, so its right edge sits at menuWidth - 1(bor | `.ink3` = `--zd-text-3` #B9B3AA = `theme(TH_TEXT_DIM)` (ui.c | WRONG IN THREE WAYS. kernel.zl:9887 `ui_caps(mx + mw - 74*u, | N/A |
| 7 | HOVER `.mi:hover` (shared). The row inverts to the knockout  |  | no geometry change — same 24px box, same padding, no inset, no bar, no radius. T | background `--zd-knock` #B6B0AB = `theme(TH_KNOCK)`; row tex | WRONG FILL, AND AN EXTRA MARK THAT SHOULD NOT EXIST. kernel. | N/A |
| 8 | DISABLED `.mi.dis` (shared). A row that states a fact instea |  | same 24px box, same padding, same bottom rule. `cursor:default`. `.mi.dis:hover  | text `--zd-text-inert` #6E685F = `theme(TH_SURF_7)` (ui.c:18 | NEARLY RIGHT, WRONG RUNG. kernel.zl:9876 `if ov_menu_dis(mi) | N/A |
| 9 | THE DISABLED-HOVER DEFECT (shared) — a real CSS specificity  |  | n/a | `.mi.dis:hover` (prototype:954) resets the ROW's background  | NOT REPRODUCED, and cannot be: zlOS has no descendant-select | N/A |
| 10 | THE SEPARATOR `.msep` (shared). A struck rule between groups | the array literal is the one-character string "-" — but see the backin | height 2, FULL BLEED — no left or right inset, it runs the entire inner width of | `--zd-lit` #6F6864 = `theme(TH_LIT)` (TH id 31, kernel.zl:10 | INVERTED — WRONG COLOUR, WRONG WEIGHT, WRONG EXTENT, WRONG B | N/A |
| 11 | FIELD MENU — HEADER TEXT. The `.mh` band's label for #ctxmen | field | as element 3: 24 tall, x 11, vertically centred, rendered UPPERCASE by `.t-lab`' | `--zd-knock-ink` on `--zd-knock`, 8.53:1 dark / 8.70:1 light | CORRECT TEXT, WRONG DRESS. kernel.zl:9861 `ui_caps(mx + 10*u | N/A |
| 12 | FIELD MENU row 0 — opens the activities overview. | activities | standard `.mi`: 24 box / 25 pitch, label at x 11 in 13/18 sans, key right-flushe | label `--zd-text-1`; key `--zd-text-3`. Enabled. | PRESENT AND CORRECT. `ov_ctx_label(0)` = "activities" (kerne | N/A |
| 13 | FIELD MENU row 1 — opens the command palette. | command palette | standard `.mi`. | label `--zd-text-1`; key `--zd-text-3`. Enabled. | PRESENT AND CORRECT. kernel.zl:9733 / 9757 / 9766 ("CTRL K") | N/A |
| 14 | FIELD MENU row 2 — SEPARATOR. | - | 2 tall, full bleed, per element 10. Sits under row 1's 1px cut rule. | `--zd-lit` / `theme(TH_LIT)`. | PRESENT at the right index — `ov_ctx_sep(2)` = 1 (kernel.zl: | N/A |
| 15 | FIELD MENU row 3 — tiles all windows onto the module grid. | tile onto the module | standard `.mi`. NO key column — the value span is emitted but empty. | label `--zd-text-1`. Enabled. | PRESENT AND CORRECT. kernel.zl:9735; `ov_ctx_haskey(3)` corr | N/A |
| 16 | FIELD MENU row 4 — toggles the ruled module grid on the desk | toggle desk grid | standard `.mi`. | label `--zd-text-1`; key `--zd-text-3`. Enabled. | PRESENT AND CORRECT. kernel.zl:9736 / 9759 / 9767 ("CTRL G") | N/A |
| 17 | FIELD MENU row 5 — toggles the knockout treatment system-wid | toggle the knockout | standard `.mi`. NO key column. | label `--zd-text-1`. Enabled. | PRESENT AND CORRECT. kernel.zl:9737, no key. | N/A |
| 18 | FIELD MENU row 6 — SEPARATOR. | - | 2 tall, full bleed. | `--zd-lit`. | PRESENT at the right index — `ov_ctx_sep(6)` = 1 (kernel.zl: | N/A |
| 19 | FIELD MENU row 7 — switch to workspace 1. | workspace 01 | standard `.mi`. Two-digit zero-padded number, matching the register-row numberin | label `--zd-text-1`; key `--zd-text-3`. Enabled. | PRESENT AND CORRECT. kernel.zl:9739 / 9760 / 9768 ("ALT 1"). | N/A |
| 20 | FIELD MENU row 8 — switch to workspace 2. | workspace 02 | standard `.mi`. | label `--zd-text-1`; key `--zd-text-3`. Enabled. | PRESENT AND CORRECT. kernel.zl:9740 / 9761 / 9769 ("ALT 2"). | N/A |
| 21 | FIELD MENU row 9 — SEPARATOR. | - | 2 tall, full bleed. | `--zd-lit`. | PRESENT at the right index — `ov_ctx_sep(9)` = 1 (kernel.zl: | N/A |
| 22 | FIELD MENU row 10 — opens the settings window. **MISSING FRO | open 11 settings | standard `.mi`. Note the label embeds the settings app's register number 11 as a | label `--zd-text-1`; key `--zd-text-3`. Enabled. | MISSING. `OV_CTX_N = 11` (kernel.zl:9730) but the prototype' | BACKABLE |
| 23 | FIELD MENU row 11 (last child) — locks the session. | lock session | standard `.mi` but `:last-child`, so NO bottom border: box height 24, not 25 pit | label `--zd-text-1`; key `--zd-text-3`. Enabled. | PRESENT as the fall-through return at kernel.zl:9742, key "C | N/A |
| 24 | WINDOW MENU — HEADER TEXT. The `.mh` band's label for #winme | composed as APP[id].n + ' ' + APP[id].name — e.g. "02 files", "11 sett | as element 3: 24 tall, x 11, uppercase, 700, tracked 1.4. Built at prototype:288 | `--zd-knock-ink` on `--zd-knock`. | WRONG — A LITERAL WHERE THE PROTOTYPE IDENTIFIES THE WINDOW. | READ |
| 25 | WINDOW MENU row 0 — raises the window. | bring to front | standard `.mi`. | label `--zd-text-1`; key `--zd-text-3`. Enabled. | PRESENT AND CORRECT. kernel.zl:9776 / 9791 / 9796 ("CLICK"). | N/A |
| 26 | WINDOW MENU row 1 — minimises the window to its register slo | minimise | standard `.mi`. | label `--zd-text-1`; key/value `--zd-text-3`. Enabled. | LABEL PRESENT, VALUE COLUMN MISSING. kernel.zl:9777 has the  | BACKABLE |
| 27 | WINDOW MENU row 2 — maximises the window. | maximise | standard `.mi`. | label `--zd-text-1`; key `--zd-text-3`. Enabled. | PRESENT AND CORRECT. kernel.zl:9778 / 9793 / 9797 ("DOUBLE C | N/A |
| 28 | WINDOW MENU row 3 — SEPARATOR. | - | 2 tall, full bleed. | `--zd-lit`. | PRESENT at the right index — `ov_win_sep(3)` = 1 (kernel.zl: | N/A |
| 29 | WINDOW MENU row 4 — tiles all windows onto the module grid.  | tile onto the module | standard `.mi`. NO key column. | label `--zd-text-1`. Enabled. | PRESENT AND CORRECT. kernel.zl:9779, haskey 0. | N/A |
| 30 | WINDOW MENU row 5 — DISABLED. States whether this plate curr | two variants, exactly: "this plate is over another" when overlapping,  | standard `.mi` box, but `.dis`: cursor default, no hover fill. Value column righ | label `--zd-text-inert` #6E685F (1.73:1 on ZD_FLOAT — delibe | HALF PRESENT, AND THE HALF THAT IS PRESENT IS PINNED TO ONE  | READ |
| 31 | WINDOW MENU row 6 — DISABLED. States whether this window's h | two variants, exactly: "header is knocked out" when this window has fo | standard `.mi` box, `.dis`. Value column right-flushed, mono, four characters (" | label `--zd-text-inert`; value `.t-num .ink3` = `--zd-text-3 | MISSING. `OV_WIN_N = 8` (kernel.zl:9774) against the prototy | READ |
| 32 | WINDOW MENU row 7 — SEPARATOR. | - | 2 tall, full bleed. | `--zd-lit`. | PRESENT but at the WRONG INDEX once element 31 is inserted:  | N/A |
| 33 | WINDOW MENU row 8 (last child) — closes the window. | close | standard `.mi` but `:last-child`, so NO bottom border: 24 tall, not 25 pitch. NO | label `--zd-text-1`. Enabled. | PRESENT as the fall-through return at kernel.zl:9782, haskey | N/A |
| 34 | THE TWO LIVE ROWS AS A PAIR — the reason this menu exists, a |  |  |  | Rows 5 and 6 are the only rows in EITHER menu that print a m | READ |
| 35 | MENU WIDTH — measured, not declared. The last thing to build |  | `min-width: 214` is a FLOOR. `.mi` is a flex row and the box is a block, so actu |  | FIXED AT 210. kernel.zl:9804 `OV_MENU_W = 210`. Two problems | BACKABLE |

**Build order.** Build in this order, because each step is verifiable on screen before the next one can lie about it.

1. **Chrome first (elements 1-2).** Ground -> ring -> radius -> shadow, and the clamp. Getting `TH_PANEL_HI` -> `TH_SURF_5` fixed here is the single highest-value edit in the whole surface: today the menu is drawn on ZD_RAISE (the on-a-plate rung), which is the one rung it must never be on, because a menu is off the plane. Nothing else matters until the ground is right.

2. **Row grammar (elements 4-10).** Row box, label span, value span, hover, disabled, the disabled-hover defect, separator. Do the separator LAST inside this group — it is the one place zlOS is not merely wrong but inverted (a 1px inset dark rule where the prototype has a 2px full-bleed light rule), so it is the easiest regression to spot and the easiest to fix wrong.

3. **Header band (element 3) + the two header texts (11, 24).** This needs the knockout fill and the reversed ink, and the window-menu header needs `slot_of_app()` + `app_name()` wired. Do the field menu's `"FIELD"` first (it is a literal, zero risk), then the window one.

4. **Field-menu rows (12-23).** Twelve entries, all static, all literals. The only work is adding the missing thirteenth slot — `open 11 settings` / `ALT 11` — which means `OV_CTX_N` 11 -> 12 and reindexing `ov_ctx_sep`/`ov_ctx_haskey`/`ov_ctx_key`. Do this before the window menu because it is pure table edit with no new reads, so it proves the row grammar from step 2 in isol

**Spot-check disagreed:** Entry 1 (THE MENU BOX) is exact — every value confirmed at presswork-prototype.html:939-943, tokens at :80 (--zd-float #4B4440), :88 (--zd-edge-over #A09A95), :150 (--zd-lift #0000008C), :180 (--zd-r-inset calc(4px*var(--ui))), :284 (light #4A423659); ui.c:186 is verbatim "theme.surf_5    = ZD_FLOAT;       /* menus, toasts - off the plane       */"; kernel.zl:104 is "TH_EDGE_OVER = 33". The 5.97:1 ring figure I reproduced myself as 5.9701 (#A09A95 on #241D19, WCAG) but it appears NOWHERE in the repo — it is derived, not read; the prototype's own comment at :86-87 states a different worst case, 3.4322:1 on ZD_FLOAT, which I also reproduced. Nit: the border "1px" is a raw unscaled px while every other number in the rule is calc(Npx * var(--ui)).  ENTRY 2 IS WRONG on one value. Source, pressw

## lock — 25 elements, 10 missing

Drawn today by: kernel/src/kernel.zl:10018 `fn ov_draw_lock(u)` (constant `OV_LOCK_W = 460` at kernel/src/kernel.zl:10017). Reached from kernel/src/kernel.zl:10097 inside `overlay_draw`, which first paints a 5/8 darkening scrim at kernel/src/kernel.zl:10089 (`shade(..., OV_SCRIM_NUM, OV_SCRIM_DEN)`, constants at 96

| # | element | exact text | geometry | tokens | zlOS today | backing |
|---|---|---|---|---|---|---|
| 1 | #lock — the full-screen field the sheet sits on. NOT a scrim |  | position:fixed; inset:0 — full screen, 0dp inset on all four sides. No radius, n | ground: --zd-void (#241D19 dark / #D8D3C9 light) = TH_BG (ui | MISSING as specified. overlay_draw (kernel.zl:10078-10091) i | READ |
| 2 | #lock::before — THE RADIAL GLOW. One off-screen-upper-left e |  | position:absolute; inset:0 (same box as #lock, i.e. the whole screen). The gradi | ink: --zd-litsoft (#5C5551 dark / #FFFCF2 light) = TH_LITSOF | MISSING. ov_draw_lock draws no glow at all. | READ |
| 3 | .sheet — the plate itself. Fixed width, height driven by con |  | width 660dp exactly (`calc(660px * var(--ui))`), height auto from content. Centr | ground: --zd-base (#322B27 dark / #EBE6DC light) = TH_PANEL  | WRONG on four counts. kernel.zl:10017-10023: width is OV_LOC | READ |
| 4 | .hdr — the KNOCKED-OUT header band. A solid light plate with |  | height 28dp exactly (--zd-title-h; `flex: 0 0 var(--zd-title-h)` so it never com | ground: --zd-knock (#B6B0AB dark / #47433D light) = TH_KNOCK | PARTIAL. kernel.zl:10028 `fill_rgb(lx, ly, lw, hh, theme(TH_ | READ |
| 5 | .hdr .ttl — the sheet's name, reversed out of the knockout.  | session locked | First child of the header, so its ink starts at 11dp from the sheet's inner left | ink: --zd-knock-ink (#181411 dark / #F4F1E9 light) = TH_KNOC | PARTIAL, wrong size. kernel.zl:10029 `ui_caps(lx + 13*u, ly  | READ |
| 6 | .hdr .sub — the seat, mono, secondary on the knockout. Sits  | tty1 · seat0 | 8dp gap after .ttl. font-family mono, font-size 11dp (--fs-sm), line-height 15dp | ink: --zd-knock-ink2 (#46413D dark / #B6B1A7 light) = TH_KNO | PARTIAL. kernel.zl:10030 `label(lx + 150*u, ly + 9*u, "tty1  | READ |
| 7 | .hdr .crd — the CODE CELL. A measured contrast ratio, right- | 6.48:1 | margin-left:auto, so it is pushed hard right and its right edge lands 6dp inside | ink: --zd-knock-ink2 (TH_KNOCK_INK2, 36), inline at prototyp | MISSING entirely. ov_draw_lock draws no .crd. | READ |
| 8 | The header's bottom edge — a 1dp rule in the knockout's own  |  | 1dp high, full sheet width, at y = header top + 28dp - 1. In light mode the shee | --zd-ko-edge (#6F6864 dark — the same value as --zd-lit — /  | MISSING. kernel.zl:10026 draws one TH_CUT hairline at the sh | READ |
| 9 | .trun — the STRUCK TOP RUN. A 1dp horizontal hairline along  |  | position:absolute; top:0; left:9dp; right:9dp (both = --zd-r-plate); height 1dp; | DARK: --zd-knock (TH_KNOCK, 34). LIGHT: --zd-ko-edge (TH_KO_ | MISSING. | READ |
| 10 | .lrun — the GRAZED LEFT RUN. A 1dp vertical hairline down th |  | position:absolute; left:0; width 1dp; top:28dp (set inline as `top:var(--zd-titl | --zd-litsoft (#5C5551 dark) = TH_LITSOFT (32). IN LIGHT IT I | MISSING. | READ |
| 11 | .fbar — THE VERMILION FOCUS BAR. Overprint job 1. A VERTICAL |  | position:absolute; left:0; top:28dp (--zd-title-h); bottom:0; width 3dp (--zd-fo | --zd-verm (#E8734F dark / #B7311A light) = TH_ACCENT (5). 4. | WRONG SHAPE, and it is the most serious divergence on the sh | READ |
| 12 | .wbody — the body box everything below lives in. A flex colu |  | padding-top 6dp (inherited from the class default `padding: 6dp 9dp 6dp`); paddi | no ground of its own — the plate's --zd-base shows through.  | IMPLICIT and inconsistent. ov_draw_lock has no body box; eve | READ |
| 13 | Row 1, cell 1 — THE CLOCK. The largest cell the sheet draws, | 14:32 | Row container: `.row` with inline `gap:14dp; align-items:flex-end; padding-top:8 | ink: --zd-text-0 (#ECE6DD dark / #1C1815 light) via class `i | PRESENT, correct in kind, far too small. kernel.zl:10036 `dr | READ |
| 14 | Row 1, cell 2 — the OPERATOR stack. A two-line label/value p | operator / root | 14dp after the clock (the row's inline gap). `.stack` = display:flex; flex-direc | label ink: --zd-text-3 (#B9B3AA dark / #56514D light) = TH_T | PRESENT, hard-positioned. kernel.zl:10037-10038: `ui_caps(lx | READ |
| 15 | Row 1, cell 3 — the LADDER stack. Same two-line shape as ele | ladder / presswork | `.stack.spread` with inline `text-align:right`. `.spread` is margin-left:auto, s | label ink --zd-text-3; value ink --zd-text-1, mono face. | MISSING entirely. | CANNOT |
| 16 | The region rule under row 1. Not a hairline — the 2dp rank b |  | `<div class="r2">`: height 2dp, flex:none, full body width (631dp — it is a flex | --zd-lit (#6F6864 dark / #FFFEF4 light) = TH_LIT (31). | WRONG WEIGHT AND WRONG TOKEN. kernel.zl:10040 `fill_rgb(lx + | READ |
| 17 | kv row 1 of 4 — UPTIME. The .kv primitive: a fixed-width upp | uptime / 4 h 12 m | `.kv`: display:flex; align-items:BASELINE (not center); gap 8dp; min-height 19dp | key ink --zd-text-3; value ink --zd-text-0 (the emphasis run | PRESENT but in a different shape and different units. kernel | READ |
| 18 | kv row 2 of 4 — WORKSPACES. Two figures in one value cell: h | workspaces / 2, 5 plates held | Identical .kv geometry to element 17. Value string is built as `'2, ' + Object.k | as element 17. | PRESENT, three fields, all left-aligned at literal x. kernel | BACKABLE |
| 19 | kv row 3 of 4 — DISPLAY. Mode, refresh rate to three decimal | display / 1600 x 1000 at 59.998 Hz, psr idle | Identical .kv geometry. Note the spacing verbatim: spaces around the 'x', ' at ' | as element 17. | PARTIAL — half the figure. kernel.zl:10057-10061 prints px_w | BACKABLE |
| 20 | kv row 4 of 4 — STORAGE. The filesystem's superblock generat | storage / rd0 generation 3, journal clean | Identical .kv geometry. Last kv in the group, so its 1dp underline is the last h | as element 17. | PRESENT but reporting a different fact, and it already has t | BACKABLE |
| 21 | The PASSPHRASE WELL — a sunken field containing a label, the | passphrase / ••••••••••• / OK | `.well` with inline `margin-top:8dp` (on top of .wbody's 5dp gap). Well: backgro | well ground --zd-well (#17100C dark / #C2BDB3 light) = TH_PA | PRESENT and DELIBERATELY REDUCED, correctly. kernel.zl:10072 | CANNOT |
| 22 | Button row, button 1 — THE PRIMARY ACTION. Vermilion fill, d | unlock | Row container: `.row` with inline `margin-top:8dp`, default gap 8dp, align-items | fill --zd-verm = TH_ACCENT (5); border-color --zd-verm; box- | MISSING. ov_draw_lock draws no buttons at all, and there is  | BACKABLE |
| 23 | Button row, button 2 — the neutral secondary. Same box, rais | switch user | 8dp after button 1. Identical box metrics: height 22dp, padding 0 10dp, gap 6dp, | fill --zd-raise (#413A36 dark / #F4EFE5 light) = TH_SURF_5 o | MISSING. | CANNOT |
| 24 | The dismiss hint — quiet mono text pushed to the far right o | esc or enter to dismiss | Third child of the button row, class `spread t-num ink3`. `.spread` is margin-le | ink --zd-text-3 (#B9B3AA dark / #56514D light) — the label r | PRESENT, split into two fields, and it names one key not two | READ |
| 25 | The FOOTNOTE row — the sheet explaining its own strongest ma | lock / the sheet is focused, so its header is knocked out at 6.48:1 -  | Inline `height:auto` (overriding .row's implicit single-line height), `margin-to | top rule --zd-cut = TH_CUT (30); label ink --zd-text-3; body | MISSING entirely. | READ |

**Build order.** Build in paint order, because every later element is positioned off an earlier one's box, and stop at the first rung that fails.

1. **Field + glow first (elements 1-2).** They are the only two things that change the whole screen, they are the cheapest to verify (look at the corner), and the glow is the single largest visual gap — `glow()` already exists as a native and `ov_draw_lock` simply never calls it. Getting element 1 right also deletes the 5/8 `shade()` at kernel.zl:10089 for the OV_LOCK branch, which is a behavioural change worth landing alone.

2. **Plate geometry (3) before any content.** Width 460 -> 660, centre on both axes instead of `/3`, add the 9dp radius via `rrect` and the four-sided ZD_EDGE_OVER ring. Every x and y below is `lx + …`, so if the plate moves later, every fix has to be redone.

3. **Header band and its four inks (4-8).** These are one visual unit and share a baseline; splitting them produces a half-header. Element 7 (`.crd`) is the one that proves `ui_ratio`/`pw_rat` are wired, and it is the cheapest true reading on the sheet — do it here, not last.

4. **The two runs and the focus bar (9-11).** Element 11 is a *correctness* fix, not a cosmetic one: the current horizontal full-width vermilion bar violates the stated width rule (design.h: "vermilion never fills an area wider than the focus bar"). Land it early so nobody builds body layout against the wrong left inset — the body's left padding is `13dp + focus_bar`, which only makes sense once t

**Spot-check disagreed:** Entry 2 is exact. Entries 1 and 3 each contain a materially wrong value. All line numbers below are verified reads.  === PROTOTYPE SOURCE (the only rule block that touches #lock; grep found no other) === docs/design/presswork-prototype.html:929-936, verbatim:   929 | #lock { position: fixed; inset: 0; z-index: 320; display: none; background: var(--zd-void); }   930 | #lock.on { display: flex; align-items: center; justify-content: center; }   931 | #lock::before { content: ''; position: absolute; inset: 0;   932 |   background: radial-gradient(108% 118% at -6% -10%,   933 |               color-mix(in srgb, var(--zd-litsoft) 40%, transparent) 0%, transparent 68%); }   934 | #lock .sheet { position: relative; width: calc(660px * var(--ui)); background: var(--zd-base);   935 |                b

## toast — 33 elements, 27 missing

Drawn today by: Drawn today by C, not zl: kernel/src/graphics/windowing/wm.c:3350 `toast_draw()` (called wm.c:3656, last in every damage rect). Geometry wm.c:3319 `toast_rect()` -> kernel/src/graphics/ui/notify.c:170 `notify_rect()`. Entry offset wm.c:3329 `toast_dy()`. Damage + animation start wm.c:4375-4397. Queu

| # | element | exact text | geometry | tokens | zlOS today | backing |
|---|---|---|---|---|---|---|
| 1 | #toasts — the stack container. Bottom-right of #field (NOT o |  | position:absolute; right:14dp; bottom:14dp; z-index:280; display:flex; flex-dire | gap = --zd-gap (design.h:429 ZD_GAP 8). Container has no gro | MISSING as a stack. notify.c shows exactly ONE toast at a ti | N/A |
| 2 | .toast — the panel itself. Off the plane: this is one of the |  | width 340dp (fixed — not min/max, every toast is the same width). border 1px sol | ground --zd-float #4B4440 (design.h:123 ZD_FLOAT, already ex | WRONG on four of four surfaces. wm.c:3378 draws the outer rr | N/A |
| 3 | The drop shadow. The one shadow in the system, and it never  |  | box-shadow: 0 6dp 14dp <lift>. Offset-y 6dp, blur 14dp, no spread, no x offset.  | --zd-lift = #0000008C in dark (proto:150) = ZD_LIFT 0x000000 | WRONG offsets. wm.c:3377 calls fb_shadow(x,y,w,h, SHADOW_OFF | N/A |
| 4 | @keyframes rise — the entry animation. Defined once in the w |  | animation: rise 90ms cubic-bezier(0.200, 0.850, 0.300, 1.000). from { opacity: 0 | duration --ms-rise 90ms (design.h:481 ZD_MS_RISE 90 — the va | WRONG on both numbers. wm.c:3329 toast_dy() uses EASE_TOAST_ | N/A |
| 5 | .toast .bar — the vermilion accent stripe down the left edge |  | position:absolute; left:0; top:0; bottom:0; width 3dp. Absolute against the PADD | fill --zd-verm #E8734F (design.h:309 ZD_VERM, theme.accent,  | PRESENT BUT WRONG WIDTH. wm.c:3385 `fb_fill_px(x + 1, y + 1, | N/A |
| 6 | .row — the title row. A flex line holding the title on the l |  | display:flex; align-items:center; gap:8dp; flex:none. Height = the 15dp line-hei | gap 8dp = --zd-gap / ZD_GAP (design.h:429). | MISSING. wm.c:3386-3388 draws exactly one line of proportion | N/A |
| 7 | The title. Rendered UPPERCASE by the label style even though | (varies — see elements 12-32; the span is `<span class="t-lab" style=" | font-size 11dp, line-height 15dp, font-weight 700, letter-spacing +1.4dp, margin | ink --zd-text-1 #DAD4CB (design.h:282 ZD_TEXT_1, theme.text, | MISSING as a distinct block, but the ENGINE EXISTS. ui_caps( | N/A |
| 8 | `dismiss` — the only interactive element on the toast. Right | dismiss | font-family mono, font-size 11dp, line-height 15dp, tabular-nums, NO uppercase t | ink --zd-text-3 #B9B3AA (design.h:284 ZD_TEXT_3, theme.text_ | MISSING as a label; PRESENT as behaviour. notify.c:139 notif | N/A |
| 9 | The body. A block-level mono run under the title row, wrappi | (varies — see elements 12-32; the span is `<span class="t-num ink2" st | display:block; font-family mono; font-size 11dp; line-height 15dp; tabular-nums. | ink --zd-text-2 #C8C2B9 (design.h:283 ZD_TEXT_2, theme.text_ | MISSING, and this is the one genuinely structural gap. Three | BACKABLE |
| 10 | Dwell, eviction and dismissal — the toast's whole lifecycle. |  | Dwell 8000 ms per toast, started at append (proto:2678: `setTimeout(() => { if ( |  | THREE-WAY DISAGREEMENT on the dwell and a different queue mo | READ |
| 11 | Scene-change wipe. Every scene switch clears the stack befor |  | $('#toasts').innerHTML = '' inside clearAll(), proto:3030. baseDesktop() calls c |  | MISSING. notify.c has notify_reset() (notify.c:73) which doe | N/A |
| 12 | STRING 01 of 21 — minWin(), proto:2263-2264. Fires on minimi | title: `minimised`  \|  body: APP[id].n + ' ' + APP[id].name.toUpperCa | title 1 line; body ~4 lines at 315dp | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | READ |
| 13 | STRING 02 of 21 — runPalette(), proto:2609. Fires when a com | title: `ran`  \|  body: c[0] + '  -  ' + c[1]  — the command name, TWO | title 1 line; body 1 line | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | N/A |
| 14 | STRING 03 of 21 — setWs(), proto:2732-2733. Fires on workspa | title: 'workspace 0' + n  — i.e. `workspace 01` or `workspace 02`  \|  | title 1 line; body ~4 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | READ |
| 15 | STRING 04 of 21 — tileAll(), proto:2756-2758. Fires on tile- | title: `tiled onto the module`  \|  body: list.length + ' plates on '  | title 1 line; body ~5 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | READ |
| 16 | STRING 05 of 21 — act('tog','knockout') true branch, proto:2 | title: `knockout on`  \|  body: `the focused header inverts to ZD_KNOC | title 1 line; body ~4 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | READ |
| 17 | STRING 06 of 21 — act('tog','knockout') false branch, proto: | title: `knockout off`  \|  body: `fallback is graphite's ZD_FOCUS_WASH | title 1 line; body ~5 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | READ |
| 18 | STRING 07 of 21 — act('tog','over') true branch, proto:2789- | title: `occlusion edge on`  \|  body: `a plate over a plate draws ZD_E | title 1 line; body ~4 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | READ |
| 19 | STRING 08 of 21 — act('tog','over') false branch, proto:2793 | title: `occlusion edge off`  \|  body: `fallback is ZD_CUT at 1.47:1.  | title 1 line; body ~3 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | READ |
| 20 | STRING 09 of 21 — act('tog','track') true branch, proto:2796 | title: `tracking on`  \|  body: `LABEL +1.4 dp, PLATE +2.6 dp. free in | title 1 line; body ~2 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | READ |
| 21 | STRING 10 of 21 — act('tog','track') false branch, proto:279 | title: `tracking off`  \|  body: `this is what the direction looks lik | title 1 line; body ~5 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | N/A |
| 22 | STRING 11 of 21 — act('seg','ladder:raking'), proto:2803-280 | title: 'ladder: ' + p[1] -> `ladder: raking`  \|  body: `graphite's si | title 1 line; body ~5 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | BACKABLE |
| 23 | STRING 12 of 21 — act('seg','ladder:presswork'), proto:2808- | title: `ladder: presswork`  \|  body: `widened. smallest step 1.1682:1 | title 1 line; body ~3 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | READ |
| 24 | STRING 13 of 21 — unlock. TWO call sites, identical pair: pr | title: `unlocked`  \|  body: `session resumed on tty1, seat0` (30 char | title 1 line; body 1 line | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | CANNOT |
| 25 | STRING 14 of 21 — act('toast','build'), proto:2824-2825. Fir | title: `rebuilt`  \|  body: `zlos.elf 249.8 KiB, text 218432 data 3072 | title 1 line; body ~2 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | CANNOT |
| 26 | STRING 15 of 21 — act('toast','stat'), proto:2826. Fires fro | title: `stat`  \|  body: FILES[S.fileSel][0] + ', generation 3, 1 exte | title 1 line; body ~2 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | CANNOT |
| 27 | STRING 16 of 21 — act('toast', anything else), proto:2827. F | title: `block re-read`  \|  body: `pipe A read back, PSR idle, 241690  | title 1 line; body ~2 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | BACKABLE |
| 28 | STRING 17 of 21 — setTheme(..., announce) dark branch, proto | title: `graphite, widened`  \|  body: `ground L* 18.15. the struck edg | title 1 line; body ~4 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | BACKABLE |
| 29 | STRING 18 of 21 — setTheme(..., announce) light branch, prot | title: `work light`  \|  body: `ground L* 91.43. no headroom above it, | title 1 line; body ~4 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | CANNOT |
| 30 | STRING 19 of 21 — setScene('desktop'), proto:3061-3066. The  | title: `five plates, one focused and over two`  \|  body: `13 REGISTER | title 37 chars — the one title that overruns the 315dp column at SM tracked; bod | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | READ |
| 31 | STRING 20 of 21 — setScene('files'), proto:3078-3081. | title: `selected`  \|  body: `fb.c 68.4 KiB, opened in 04 EDITOR. the  | title 1 line; body ~6 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | BACKABLE |
| 32 | STRING 21 of 21 — setScene('monitor'), proto:3087-3090. | title: `sampling at 1 Hz`  \|  body: `03 MONITOR is focused and lies u | title 1 line; body ~7 lines | title ZD_TEXT_1, body ZD_TEXT_2 | MISSING | N/A |
| 33 | DISAGREEMENT LEDGER — design.h's ZD_TOAST_* block (design.h: |  | ZD_TOAST_W 300 \| proto 340 \| notify.c NOTE_W 280 — THREE VALUES, none agree. Z |  | Six of nine ZD_TOAST_* constants are wrong, two of the three | N/A |

**Build order.** Do it in this order, and do NOT start with the strings.

1. Fix the three-way disagreement first (elements 1-5, 9). design.h is wrong about the toast in six of nine constants and notify.c is wrong in three more; every later step reads those numbers. Land ZD_TOAST_W 340, ZD_TOAST_PT/PR/PB/PL 6/11/7/14, ZD_TOAST_BAR = ZD_FOCUS_BAR, ZD_TOAST_MS 8000, ZD_TOAST_INSET 14, ZD_TOAST_FROM_DY 4, ZD_MS_TOAST = ZD_MS_RISE (90), and new ZD_LIFT_OVER_DY 6 / ZD_LIFT_OVER_BLUR 14. Delete ZD_TOAST_ICON / ZD_TOAST_ICON_R (the prototype has no icon; the accent bar replaced it) and ease.h:45 EASE_MS_TOAST (dead - wm.c:1086 uses EASE_MS_OV). Nothing renders yet, nothing can break.

2. Repaint the existing single-line toast against those constants (elements 2,3,5 in wm.c:3350). Four token swaps - panel_hi->surf_5, border->edge_over, t->radius->ZD_R_INSET, UI_S1(t)/2->theme.focus_bar - plus the overlay shadow. All four target roles already exist in ui.c:154-198. Smallest visible correctness win in the file; do it before any structural change so you can see the ladder land.

3. Then the stack (element 1) and the dwell (element 9), because they are the reason notify.c exists and they are the only genuinely structural change: one-visible-of-four becomes three-visible-of-N, and NOTE_H 56 becomes content-derived. Everything after this is layout inside a rectangle whose size you now control.

4. Then the two-block content (elements 6,7,8) - ui_caps() already bakes ZD_TR_LAB, so the title is one call; the

**Spot-check disagreed:** Entry 2 is wrong (one number). Entries 1 and 3 are exact on every value they state, but each has a material omission a builder would trip on.  === 1. #toasts — EXACT (with one omission) === Read proto:958-960 verbatim:   #toasts { position: absolute; right: calc(14px * var(--ui)); bottom: calc(14px * var(--ui));             z-index: 280; display: flex; flex-direction: column; gap: var(--zd-gap);             align-items: flex-end; } Every geometry claim matches. Containment ✔ — `<div id="toasts"></div>` at proto:1025 sits inside `<div id="field">` (opened proto:1013, closed before `<div id="foot">` at proto:1027), and #field is `position: relative` (proto:526), so it IS the positioned ancestor: 14dp is from the field's edges, foot/rail/strip excluded. Cap ✔ — proto:2677-2678:   $('#toasts')

## scroll-and-bars — 24 elements, 2 missing

Drawn today by: kernel/src/graphics/ui/ui.c:1232 (ui_scroll_begin) and kernel/src/graphics/ui/ui.c:1265 (ui_scroll_end) are the toolkit's scroll region; the zl bridge is ui.c:1314 ui_scroll_begin_value / ui.c:1319 ui_scroll_end_value, bound as the natives `ui_scroll` / `ui_scroll_end` / `ui_scroll_content` at frees

| # | element | exact text | geometry | tokens | zlOS today | backing |
|---|---|---|---|---|---|---|
| 1 | ::-webkit-scrollbar — the scrollbar gutter itself. Applies G |  | width: 9dp, height: 9dp (calc(9px * var(--ui)) both). At --ui: 1 that is 9 CSS p | no colour of its own; the gutter is whatever -track paints.  | ui.c:1273 `int bw = UI_S1(&theme);` — UI_S1 is UI_DP(t,4) (u | N/A |
| 2 | ::-webkit-scrollbar-track — the sunken channel the thumb run |  | fills the 9dp gutter for the full height of the scrollport. NO border-radius — t | background: var(--zd-well) = #17100C dark / #C2BDB3 light. T | ui.c:1279 `fb_rrect(bx, S.y, bw, S.h, bw / 2, theme.panel_hi | N/A |
| 3 | ::-webkit-scrollbar-thumb — the grip. Its LENGTH is the one  |  | width 9dp (the gutter). border-radius: var(--zd-r-chip) = 2dp, a FIXED 2dp regar | background: var(--zd-text-inert) = #6E685F dark / #88837B li | ui.c:1280 `fb_rrect(bx, ty, bw, th2, bw / 2, theme.text_dim) | READ |
| 4 | #reg — the rail's REGISTER list, the first scrolling surface |  | overflow-y: auto; overflow-x: hidden; flex: 1 1 auto; min-height: 0. Sits betwee | no ground of its own — inherits #rail's var(--zd-raise). Row | draw_rail_reg, kernel.zl:1583. It does NOT scroll — it TRUNC | READ |
| 5 | .well — the sunken container. Not itself a scrolling surface |  | border-radius: var(--zd-r-inset) = 4dp. border: 1px solid. padding: 6dp 8dp (ver | background: var(--zd-well) #17100C. border: 1px var(--zd-cut | pw_well, kernel.zl:2951-2958. CORRECT, four calls, token for | N/A |
| 6 | .scroll — the viewport. The class that turns a stack into a  |  | flex: 1 1 auto; min-height: 0; overflow-y: auto; overflow-x: hidden. min-height: | no ground of its own — it sits directly on the plate, var(-- | MISSING as a shared primitive. ui_scroll_begin (ui.c:1232) i | READ |
| 7 | .scroll::after — THE TRIM RULE. The whole point of this surf |  | content: ''; display: block; position: sticky; bottom: 0; height: calc(2px * var | background: var(--zd-lit) = #6F6864 dark, 2.5423:1 on ZD_BAS | EXISTS EXACTLY ONCE IN THE WHOLE TREE: kernel/src/kernel.zl: | N/A |
| 8 | th — the sticky column head. It is part of the scrolling sur | column head labels are per-table and uppercased by CSS from lowercase  | position: sticky; top: 0. font-size var(--fs-sm) 11dp / line-height var(--lh-sm) | background: var(--zd-base) #322B27 (it has to be opaque or r | ui_colhead, kernel/src/graphics/ui/uikit.c:1257. The colours | N/A |
| 9 | td — the data row, because the trim's meaning depends on it. |  | font var(--mono), font-size var(--fs-sm) 11dp, line-height var(--lh-sm) 15dp, pa | colour var(--zd-text-2) #C8C2B9. border-bottom 1px var(--zd- | CORRECT and already measured off the render: design.h:632-63 | N/A |
| 10 | #presults — the command palette's result list. The SECOND un |  | max-height: calc(300px * var(--ui)) = 300dp; overflow-y: auto. Does NOT set over | sheet ground var(--zd-float) #4B4440. row groove 1px var(--z | MISSING — there is no command palette in the zl tree. Nothin | N/A |
| 11 | R.shell's transcript — the THIRD untrimmed scrolling surface |  | prototype 1650: <div class="well stack" style="flex:1;min-height:0;overflow:auto | .well's: ground var(--zd-well), 1px var(--zd-cut) border, in | app_draw's APP_SHELL arm, kernel.zl:9096-9110. The transcrip | READ |
| 12 | .well.scroll — the ONE place the two classes combine, in R.e |  | prototype 1706: <div class="well scroll" style="padding:calc(6px * var(--ui)) 0" | .well's ground var(--zd-well) and both inset runs, plus .scr | ed_text_body, kernel.zl:3116. It draws pw_well correctly (31 | READ |
| 13 | PANE AUDIT — 11 SETTINGS (APP_SETTINGS = 6). The reference i | "design.h - live" (the header line; the prototype uses a middle dot be | SET_PADX 9 / SET_PADY 6 / SET_TABROW 24 / SET_R2 2 / SET_STACK 5 / SET_SCRLSTEP  | trim theme(TH_LIT) via set_rule2 (kernel.zl:7872). Body grou | kernel.zl:8897 settings_body; scroll 8921-8928; TRIM 8930-89 | READ |
| 14 | PANE AUDIT — 02 FILES (APP_FILES = 13). Prototype counterpar | column head "name\|size\|modified" (kernel.zl:7007). The prototype's h | FILES_ROW = 24 design px (kernel.zl:6774) — note this is NOT the prototype's 20d | selected ground theme(TH_PANEL_HI) with a focus_bar()-wide t | files_draw, kernel.zl:6885. It genuinely scrolls — files_scr | READ |
| 15 | PANE AUDIT — 01 MONITOR (APP_MONITOR = 1). Prototype counter | section captions "cores", "ioapic lines", "composite time", "windows"  | two columns, colw = (mw - SM_GAP*ui)/2. Row pitch pw_rowh() = PW_ROWH 20 * ui (k | ground theme(TH_PANEL). Rules via pw_row_rule -> theme(TH_CU | sysmon_body, kernel.zl:5484. THREE silent truncations: cores | READ |
| 16 | PANE AUDIT — 10 SYSTEM (APP_SYSPANE = 71). Prototype counter | " of " / " rows fit - make the window taller" (kernel.zl:6203-6208); s | table columns 44 / 18 / 16 / 22 percent (kernel.zl:6106-6109, sysg_head), matchi | head rule theme(TH_LIT) 2dp — the same weight the trim would | sys_body kernel.zl:6154; rows sysg_rows kernel.zl:6117-6121, | READ |
| 17 | PANE AUDIT — 14 TYPE (APP_TYPEPANE = 72). Prototype counterp | section caption "every atlas linked into this image" + " rows" (kernel | columns 32 / 46 / 60 / 78 percent boundaries (ty_rows, kernel.zl:6405-6420). Row | ground theme(TH_PANEL); dimmed rows theme(TH_TEXT_DIM) for a | type_body kernel.zl:6482; ty_rows kernel.zl:6393, `if tw_y + | READ |
| 18 | PANE AUDIT — 12 EDIT (APP_EDIT = 12). Prototype counterpart: | footer "ln" / "col" / "tab" labels; binary fallback "no text preview"  | gutter width 5 * cell_w() + 8dp (kernel.zl:3122); 1px divider at tx + gw (3123); | well via pw_well; divider theme(TH_CUT); gutter numbers them | ed_text_body kernel.zl:3116; hard stop at kernel.zl:3140. NO | READ |
| 19 | PANE AUDIT — 40 KERNEL LOG (APP_KLOG = 40). Prototype counte | " of " then " lines fit - make the window taller" when short, and " li | rows walk while `ty2 + rowh <= limit` (apps_sys2.zl:362). Selected line repeated | row ink theme(TH_TEXT_2) for the subsystem, theme(TH_TEXT_HI | apps_sys2.zl:355-400. NO TRIM. The conditional message at ap | READ |
| 20 | PANE AUDIT — 42 HEX VIEWER (APP_HEXV = 42). Prototype counte | status band "kernel image - live memory - offset 0x" and "16 bytes/row | ui_grid("74\|*\|150"); 16 fixed rows; row guard `if by + lh <= py + ph` (apps_sy | ui_mono panel S2_PANEL_HEX; offsets ui_ink(S2_IK_DIM); bytes | hx_ui, apps_sys2.zl:549. Pages with three chips instead of s | READ |
| 21 | PANE AUDIT — 70 ALL APPLICATIONS (APP_CATALOG = 70). Genuine |  | tile grid, CAT_TILE_W x CAT_TILE_H, row = i/cols - cat_scroll (apps_registry.zl: | CARRIES RAW COLOUR LITERALS: bgc = rgb(26, 32, 48) and rgb(4 | cat_draw, apps_registry.zl:641; scroll offset cat_scroll; ba | READ |
| 22 | PANE AUDIT — the apps_sys3 four: 46 DISK (s3du_draw, apps_sy |  | S3AR_ROW-pitched rows against ay + ah (apps_sys3.zl:881); s3du rows at 20dp agai | theme(TH_PANEL) grounds, theme(TH_BORDER)/theme(TH_BG) for t | Four silent truncations. IMPORTANT: R.disk in the prototype  | READ |
| 23 | PANE AUDIT — the ten apps_utils panes: 15 CLIP (cb_draw:389) |  | au_fits(y, ay, ah) — apps_utils.zl:273, `if y + cell_h() > ay + ah { return 0 }` | au_note ink theme(TH_TEXT_DIM); rows via au_row_base. | No trim on any of the ten. But the shape is different from t | N/A |
| 24 | PANE AUDIT — 05 BROWSER (APP_BROWSER = 5). Scrolls, and has  |  | kernel/src/web/browser.c:1795-1802. bw = UI_S1(t) = 4dp (same wrong width as ui_ | track t->panel_hi (ZD_RAISE) — same wrong token as ui_scroll | browser.c:1791 calls fb_clip_none() immediately before drawi | READ |

**Build order.** Build in this order, and the order is chosen so nothing is drawn before the number it depends on exists.

1. ZD_SCROLLBAR_W = 9 and ZD_SCROLLBAR_MIN = 24 into design.h, next to ZD_COLHEAD_PR. Right now there is NO scrollbar token anywhere in design.h — ui.c spells the width as UI_S1 (4dp) and the min thumb as UI_S6 (24dp), which is 4dp against the prototype's 9. Add the tokens first so steps 2 and 3 have something to read.

2. Fix ui_scroll_end's three wrong values in place (ui.c:1272-1281): width 4->9, track theme.panel_hi (ZD_RAISE) -> theme.surf_well (ZD_WELL), thumb theme.text_dim (ZD_TEXT_3, a TEXT rung) -> theme.surf_7 (ZD_TEXT_INERT). The repo already argues this fix against itself at kernel.zl:1103-1110, which names ZD_TEXT_INERT's four legal jobs and puts "scrollbar thumb" first. Also drop the radius on the track (prototype's -track carries none) and keep bw/2 only on the thumb. This is a 5-line diff and it is the cheapest correctness win in the whole surface.

3. Add the trim to ui_scroll_end, immediately after the scrollbar block, inside the same `if (S.content > S.h)` guard: fb_fill_px(S.x, S.y + S.h - UI_DP(&theme,2), S.w - UI_DP(&theme,9), UI_DP(&theme,2), theme.lit). Guarding it on content>viewport is not an optimisation, it is the semantics — a trim under an uncut column says "there is more" when there is not, and kernel.zl:8935 already gets this right.

4. Only now port panes. Do NOT convert them all: ui_scroll_begin/ui_scroll_end have never been executed by 

**Spot-check disagreed:** Re-derived from /home/roy/Documents/repos/zl-linux-presswork/docs/design/presswork-prototype.html. All three entries are substantively correct on geometry and selector scope. ONE VALUE IS WRONG (entry 2) and THREE LINE CITATIONS ARE WRONG. Detail:  === ENTRY 1 (::-webkit-scrollbar) — EXACT, one omission === Confirmed verbatim, line 333:   "::-webkit-scrollbar { width: calc(9px * var(--ui)); height: calc(9px * var(--ui)); }" Unscoped/global: yes, it sits in the top-level style block, no ancestor selector. text "" correct (pseudo-element, no content). 9dp both axes correct. --ui verified: ":root" declares "--ui: 1;" at line 164 (NOT 163 — 163 is the comment "/* ---- metrics */"). Slider is line 2037 (NOT 2049 — 2049 is "['--zd-grid', 'the module grid'],"): "slider('ui scale', 'ui', 80, 140, 

## window-motion — 27 elements, 2 missing

Drawn today by: SPLIT, and this matters for who you edit. The WINDOW half is C, not zl: kernel/src/graphics/windowing/wm.c — chrome_seat() :2616 (ring + trun + lrun), chrome_header() :2659 (the knockout band + its foot rule), chrome_focus_bar() :2710 (.fbar), chrome_title_run() :2673-2841 (reg/ttl/sub inks), chrome

| # | element | exact text | geometry | tokens | zlOS today | backing |
|---|---|---|---|---|---|---|
| 1 | .win — the plate itself. The absolutely-positioned window bo |  | position:absolute; display:flex; flex-direction:column; overflow:hidden. border: | ground = --zd-base #322B27 (design.h ZD_BASE :121, L* 18.15) | wm.c chrome_seat():2616-2632 draws it as exactly two calls:  | N/A |
| 2 | .win.over — the occluder's ring. Not part of focus; listed b |  | Same 1px ring, colour only. | --zd-edge-over #A09A95 replaces --zd-cut. | win_over_below(win) passed as the `over` argument through ch | READ |
| 3 | .win.max — the bolted plate. Radius drops to zero and both r |  | border-radius: --zd-r-bolt = 0px. `.win.max .trun { left: 0; right: 0; }` and `. | ZD_R_BOLT = 0 (design.h:388). | chrome_shell():3039 picks ZD_R_BOLT when W->maxed. chrome_se | READ |
| 4 | .win.drag — the lift. The ONLY shadow in the system at rest- |  | box-shadow: 0 calc(5px * --ui) calc(13px * --ui) --zd-lift. Offset 5dp down, blu | --zd-lift #0000008C (8-digit, 55% black on graphite / 35% wa | wm.c chrome_shadow():2576-2589. Guard at :2583 is `if (!moda | READ |
| 5 | .trun — THE STRUCK TOP RUN. Focus property #1. 1px along the |  | position:absolute; left: --zd-r-plate (9dp); right: --zd-r-plate; top: 0; height | unfocused: --zd-lit #6F6864 (ZD_LIT design.h:125, L* 44.52,  | wm.c chrome_seat():2663-2665 — `int run_w = W->w - 2*r; if ( | N/A |
| 6 | .lrun — THE GRAZED LEFT RUN. Focus property #2, and the only |  | position:absolute; left: 0; top: --zd-r-plate (9dp) at rest → --zd-title-h (28dp | --zd-litsoft #5C5551 (ZD_LITSOFT design.h:124, L* 36.68) in  | wm.c chrome_seat():2666-2668 — `int ly = W->y + ((focused && | N/A |
| 7 | .hdr — THE KNOCKOUT. Focus property #3 and the load-bearing  |  | height: --zd-title-h = 28dp; flex: 0 0 28dp; display:flex; align-items:center; g | unfocused: --zd-base #322B27 (the plate's own value — an unf | wm.c chrome_header():2659-2694. `fb_rrect_grad_top(ix, W->y+ | N/A |
| 8 | .hdr .reg — the register slot number. First cell of the runn | Two digits, zero-padded, verbatim from the APPS table at proto:1545-15 | font-family: --mono (ui-monospace, 'DejaVu Sans Mono', monospace); font-size: -- | ink at rest = --zd-text-3 #B9B3AA (ZD_TEXT_3, label rung, 6. | wm.c chrome_title_run():2795-2802 builds it by hand: `r[0] = | READ |
| 9 | .hdr .ttl — the window name. The header's one piece of runni | Verbatim from proto:1545-1560, lowercase in the markup, uppercased by  | font-size: --fs-md = 13dp; font-weight: 700; text-transform: uppercase; letter-s | ink at rest = --zd-text-2 #C8C2B9 (ZD_TEXT_2, secondary rung | wm.c chrome_title_run():2804-2830. Uppercases into a 32-byte | N/A |
| 10 | .hdr .sub — which instance. Mono, dim, third cell. | Verbatim from proto:1545-1560: "zlsh" "rd0" "1 Hz" "fb.c" "dmesg" "rd0 | font-family: --mono; font-size: --fs-sm = 11dp; white-space:nowrap; overflow:hid | same ink_dim as .reg: --zd-text-3 at rest, --zd-knock-ink2 f | wm.c chrome_title_run():2832-2839. Drawn only if it fits ent | READ |
| 11 | .hdr .crd — the module coordinate. The ONE thing in the whol | Format from moduleOf() proto:2282-2294: 'M' + two-digit column (1-base | margin-left: auto; font-family: --mono; font-size: --fs-sm = 11dp; flex:none; wh | same ink_dim: --zd-text-3 at rest, --zd-knock-ink2 focused. | wm.c chrome_module():2869-2900, drawn in the DIRECT pass aft | READ |
| 12 | .ctl / .cbtn — the three window controls. No face at rest: t | title attributes, verbatim: "minimise" "maximise" "close". No visible  | .ctl { display:flex; margin-left:auto; align-self:stretch; flex:none } — full he | at rest: ink --zd-text-3 (ZD_WINCTL_INK, design.h:365), rule | wm.c chrome_shell():3116-3130+, loop `for (int b = TITLE_CLO | N/A |
| 13 | .hdr border-bottom — the header's foot rule, and the one pla |  | 1px, full inner width (ix = W->x+1, iw = W->w-2), on row W->y + hh - 1. | unfocused: --zd-cut #0A0300 (the groove; from the plate ther | wm.c chrome_header():2689 — `fb_fill_px(ix, foot, iw, 1, foc | N/A |
| 14 | .hdr .cbtn under body.nokock — the fallback header's control |  | unchanged. | body.nokock .win.focus .hdr .cbtn: color --zd-text-3, border | NOT PRESENT — chrome_header() as read at wm.c:2659-2694 has  | CANNOT |
| 15 | .fbar — THE VERMILION FOCUS BAR. Overprint job 1. Focus prop |  | position:absolute; left: 0; top: --zd-title-h (28dp); bottom: 0; width: --zd-foc | --zd-verm #E8734F (ZD_VERM design.h:309, 4.6319:1 on ZD_BASE | wm.c chrome_focus_bar():2710-2718. Complete and correct incl | N/A |
| 16 | .wbody padding-left — the client area's inset, which grows b |  | at rest: padding: 6dp 9dp 6dp (top, sides, bottom); gap 5dp. Focused: `padding-l | n/a — geometry only. | Handled by client_of()'s left inset — chrome_band():3037 spe | N/A |
| 17 | .sband — the foot band. Same padding-left shift as .wbody. F | Cells left to right, from band() at proto:1620-1622: the two-digit reg | height: --zd-band-h = 20dp; flex: 0 0 20dp; align-items:center; gap 10dp; border | rule --zd-cut (1.4723:1 on the plate — a groove you feel, no | wm.c chrome_band():2951-3029. `(void)focused;` at :3032 with | READ |
| 18 | .grip — the resize corner. Not focus-sensitive; listed becau |  | position:absolute; right:0; bottom:0; 15dp x 15dp; cursor:nwse-resize; z-index:  | --zd-text-inert #6E685F — STRUCTURE ONLY, never running text | Hit region is WIDER than the glyph: in_resize_grip() at wm.c | N/A |
| 19 | .slot — the rail register row. The focus signal's rail echo. | Row content left to right: the two-digit number ("01".."14"), the icon | display:flex; align-items:center; gap: 7dp; height: --zd-row-h = 26dp; padding:  | idle ground --zd-raise #413A36 (TH_PANEL_HI). hover --zd-flo | kernel.zl draw_rail_row():1052-1114. FOUR STATES, each a lad | READ |
| 20 | .slot .mk — THE REGISTER MARK. Overprint job 1's rail half,  |  | position:absolute; left: 0; width: --zd-focus-bar = 3dp. RUNNING: top: 35%; bott | running: --zd-verm #E8734F (TH_ACCENT). focused: --zd-verm-b | kernel.zl:1069-1076, and it is COMPLETE: `mkh = rh * RAIL_MK | N/A |
| 21 | .prow .mk — the command palette's selected-row mark. Same 3d |  | .prow { display:flex; align-items:center; gap 10dp; position:relative; padding:  | unselected mark: transparent. Selected row ground: --zd-knoc | NOT VERIFIED — I did not locate the palette's draw function  | N/A |
| 22 | .thumb.on — the overview tile's focus signal. Listed because |  | .thumb .th { height: 20dp; gap 6dp; padding 0 7dp; border-bottom 1px }. .thumb.o | tile border --zd-edge-over on EVERY tile, focused or not. .t | NOT VERIFIED — did not locate the overview draw path. | N/A |
| 23 | body.nomo — the global motion kill switch, and it is a shipp | Settings row label and caption, verbatim from proto:2186: label "motio | `body.nomo * { transition: none !important; animation: none !important; }` (prot | n/a | MISSING as a user-facing switch. wm.c has no equivalent glob | BACKABLE |
| 24 | THE MOTION CONSTANTS THEMSELVES — three durations, one curve | design.h:472-479, verbatim: "RISE    90ms   the load-bearing half of t | ZD_MS_RISE 90, ZD_MS_TRAVEL 160, ZD_MS_SETTLE 240 (design.h:481-483), stored in  | n/a — timing, not colour. | MISSING — and this is the headline defect on this surface. w | BACKABLE |
| 25 | THE FOCUS CHANGE ITSELF — what wm_focus() does today, and th |  | Today: instantaneous. Nothing between state A and state B. | n/a | wm.c wm_focus():2102-2133. In order: un-minimise if minimise | BACKABLE |
| 26 | THE PROPERTIES THAT MUST STAY CUTS — the negative half of th |  | n/a | n/a | All correctly cut today. | N/A |
| 27 | THE FOCUS CHANGE FRAME BY FRAME — the reference table to bui |  | RISE 90 ms on cubic-bezier(0.200, 0.850, 0.300, 1.000). At the measured 100 Hz P | See rows above. .hdr bg is ZD_BASE→ZD_KNOCK, .trun is ZD_LIT | All nine rows collapse to row 0 → row 9 with nothing between | N/A |

**Build order.** Build in this order, and the first three are the whole visible signal.

1. FIX THE MOTION CONSTANTS FIRST — element 24. wm.c's timeline reads ease.h, and ease.h's every duration and curve is cited to ds-reference.html, the SUPERSEDED file (ease.h:23, :29, :33 name it by filename). design.h's ZD_MS_RISE/TRAVEL/SETTLE/WIN/PRESS/POP/OV/PULSE and ZD_EASE_WIN_* are referenced by NOTHING outside design.h — I grepped kernel/src and freestanding for all eight names and got zero hits. So today a window opens in 200 ms (EASE_MS_WIN) where PRESSWORK says 160 (ZD_MS_WIN = ZD_MS_TRAVEL), a press acknowledges in 250 where PRESSWORK says 90, a pulse runs 1000 where PRESSWORK says 240 and its floor is 255 (i.e. it should do nothing at all), and five curves are in play where PRESSWORK declares one. This is a pure constant edit in ease.h with no new code, it is the cheapest change in the list, and every later step depends on the numbers being the authority's. Do it before you write a line of animation.

2. THEN THE THREE CUT PROPERTIES THAT ARE PURE VALUE PICKS — elements 5, 7 and 13. The trun's ZD_LIT→ZD_KNOCK swap, the header band's ZD_BASE→ZD_KNOCK flip, and the header foot's ZD_CUT→ZD_KO_EDGE swap are already correct in wm.c as CUTS (chrome_seat:2664, chrome_header:2683, chrome_header:2686). Nothing to build; verify against the token table in element 5/7/13 and move on. They are listed because a build spec that omits the parts already right invites someone to "fix" them.

3. THE LRUN'S Y-O

**Spot-check disagreed:** TWO OF THREE ARE WRONG AS RENDERED. The token VALUES are all exact — I recomputed every colour number — but entries 2 and 3 describe CSS that the cascade kills.  === THE ROOT DEFECT (breaks entries 2 and 3) === The selector is `#wins .win`, not `.win`. proto:598-603, quoted verbatim:    #wins .win {     position: absolute; display: flex; flex-direction: column; overflow: hidden;     background: var(--zd-base); border-radius: var(--zd-r-plate);     border: 1px solid var(--zd-cut);     transition: border-color var(--ms-rise) var(--ease);   }  Specificity (1 id, 1 class) = 1,1,0. The two override rules are class-only:   proto:604  `.win.over { border-color: var(--zd-edge-over); }`   -> 0,2,0   proto:606  `.win.max  { border-radius: var(--zd-r-bolt); }`     -> 0,2,0 An id beats any number of c

## drag-bounds — 22 elements, 5 missing

Drawn today by: Primary: kernel/src/graphics/windowing/wm.c:2226 `wm_move()` — the clamp helper `clamp_to_field()` sits directly above at wm.c:2209-2223, fully written and correct, and wm.c:2229 voids it. Secondary write paths that also assign geometry with no clamp: wm.c:1919 `wm_open()` (stores x,y,w,h verbatim a

| # | element | exact text | geometry | tokens | zlOS today | backing |
|---|---|---|---|---|---|---|
| 1 | THE FIELD — the coordinate frame every clamp below is expres |  | Screen-space field origin = (rail_w, strip_h) = (170, 30) design px; field size  | ground of #field is --zd-void; #rail is --zd-raise with a 1p | PRESENT AND CORRECT as macros: wm.c:313-315. Also present zl | READ |
| 2 | THE MAXIMISE OVERRIDE, applied BEFORE the clamps so a maximi |  | `let x = w.x, y = w.y, ww = w.w, hh = w.h; if (w.max) { x = 0; y = 0; ww = fw; h | a maximised plate swaps its corner radius: `.win.max { borde | EQUIVALENT AND CORRECT. zlOS has no `max` boolean; SNAP_MAX  | READ |
| 3 | THE X CLAMP — rule 1's left/right half. Runs on EVERY layout | x = Math.max(0, Math.min(x, Math.max(0, fw - 120))); | Field-space: x is clamped to [0, max(0, fw-120)]. Screen-space: [rail_w, max(rai | n/a — geometry only. The plate it moves is --zd-base with a  | WRITTEN BUT DISABLED. wm.c:2209-2223 `clamp_to_field()` is a | READ |
| 4 | THE Y CLAMP — rule 1's top/bottom half. Same call-site and s | y = Math.max(0, Math.min(y, Math.max(0, fh - 60))); | Field-space: y clamped to [0, max(0, fh-60)]. Screen-space: [strip_h, max(strip_ | the header this protects is `.hdr` at `height: var(--zd-titl | WRITTEN BUT DISABLED, same helper. wm.c:2214-2216: `int fy = | READ |
| 5 | THE W CLAMP — a plate may not be WIDER than the field. Note  | ww = Math.min(ww, fw); | Field-space: ww clamped to [.., fw]. Screen-space cap = screen_w - rail_w. No mi | n/a | MISSING ENTIRELY. wm.c:2237-2249 `wm_resize()` applies ONLY  | BACKABLE |
| 6 | THE H CLAMP — a plate may not be TALLER than the field. Same | hh = Math.min(hh, fh); | Field-space cap fh. Screen-space cap = screen_h - strip_h - foot_h. Same line as | n/a | MISSING ENTIRELY — same gap as element 5. | BACKABLE |
| 7 | THE DRAG HANDLE — the header, and only the header. The proto | data-drag="1" | `.hdr` (653-658): `height: var(--zd-title-h)` = 28dp, `flex: 0 0 var(--zd-title- | at rest: ground --zd-base, border-bottom 1px --zd-cut, ink - | PRESENT AND CORRECT. wm.c:3823-3829 `in_titlebar()` tests `y | READ |
| 8 | THE MAXIMISED-PLATE DRAG GUARD. A maximised plate cannot be  | const w = S.wins[id]; if (!w \|\| w.max) return; | startDrag line 2902. No threshold, no un-maximise-on-drag — a hard refusal. | n/a | MISSING — AND THE BEHAVIOUR IS OPPOSITE. route_mouse wm.c:40 | BACKABLE |
| 9 | THE GRAB OFFSET — the plate keeps the grip point under the p | const ox = ev.clientX - e.offsetLeft, oy = ev.clientY - e.offsetTop; | startDrag 2904. Motion handler 2906: `function mv(m) { w.x = m.clientX - ox; w.y | n/a | PRESENT AND CORRECT. route_mouse wm.c:4050-4051 `grab_dx = x | N/A |
| 10 | THE LIFT — the one and only shadow an ordinary window ever c | e.classList.add('drag'); | `.win.drag { box-shadow: 0 calc(5px * var(--ui)) calc(13px * var(--ui)) var(--zd | --zd-lift: #0000008C (150) = black at alpha 140/255 = 55%. | PRESENT AND CORRECT, AND ALREADY COVERS RESIZE TOO. wm.c:375 | READ |
| 11 | CLAMP-PER-FRAME, NOT CLAMP-ON-DROP. Every pointer-motion fra | function mv(m) { w.x = m.clientX - ox; w.y = m.clientY - oy; layout(); | startDrag 2906 (motion) and 2907-2912 (up: `e.classList.remove('drag'); removeEv | n/a | STRUCTURALLY EQUIVALENT ALREADY — the clamp only has to go i | N/A |
| 12 | THE MODULE READOUT — the ONLY figure this whole surface prin | 'M' + ('0' + (c0 + 1)).slice(-2) + ('0' + (r0 + 1)).slice(-2) + ' ' +  | Format: `M` + 2-digit column + 2-digit row + one space + colspan + `x` + rowspan | ink --zd-text-3 at rest, --zd-knock-ink2 on a focused (knock | PRESENT AND CORRECT AND MORE CAREFUL THAN THE PROTOTYPE. wm. | READ |
| 13 | THE RESIZE GRIP — bottom-right corner only. Bottom-right is  |  | `.grip` (725-726): `position:absolute; right:0; bottom:0; width: calc(15px*var(- | stroke --zd-text-inert, 1.2px. Sits on whatever the plate's  | PRESENT BUT A DIFFERENT SHAPE — AND THE zlOS SHAPE IS THE BE | READ |
| 14 | RESIZING UN-MAXIMISES. Unlike dragging (element 8, which ref | const w = S.wins[id]; if (!w) return; w.max = false; | startResize 2918-2919. Note `w.pre` (the restore rectangle stashed by maxWin at  | n/a | MISSING. route_mouse's GRAB_RESIZE branch (wm.c:3970-3974) a | BACKABLE |
| 15 | THE RESIZE ANCHOR — the corner follows the pointer by delta  | const sx = ev.clientX, sy = ev.clientY, sw2 = w.w, sh = w.h; | startResize 2920. Deltas are `m.clientX - sx` and `m.clientY - sy` added to the  | n/a | PRESENT, DIFFERENT FORMULATION, EQUIVALENT RESULT. wm.c:4064 | N/A |
| 16 | MINIMUM WIDTH. | w.w = Math.max(280, sw2 + m.clientX - sx); | startResize 2923. 280 DESIGN PX, a hard floor, applied in the motion handler bef | n/a | PRESENT BUT WRONG UNIT AND WRONG VALUE. wm.c:1952 `wins[i].m | BACKABLE |
| 17 | MINIMUM HEIGHT. | w.h = Math.max(160, sh + m.clientY - sy); | startResize 2924. 160 DESIGN PX. 160 = title_h (28) + 132dp of body, so a minimu | n/a | PRESENT BUT WRONG UNIT AND WRONG VALUE. wm.c:1953 `wins[i].m | BACKABLE |
| 18 | RESIZE IS NOT POSITION-CLAMPED. startResize never touches w. |  | Verified by absence: startResize (2917-2933) assigns only w.w and w.h. | n/a | SAME BY ABSENCE, but with a real consequence the prototype d | BACKABLE |
| 19 | THE OCCLUSION EDGE, recomputed after the clamp on every layo | if (S.cfg.over) { … if (overlaps(me, {x: oe.offsetLeft, y: oe.offsetTo | layout() 2340-2352. `me` is built from the CLAMPED x, y, ww, hh (2339). The list | `.win.over { border-color: var(--zd-edge-over); }` (604), re | PARTIALLY PRESENT — the token and the seat exist (UI_COLOR_E | READ |
| 20 | THE OPEN-TIME POSITIONS. Every default rect in the prototype | S.wins[id] = { id: id, x: r[0], y: r[1], w: r[2], h: r[3], ws: ws \|\| | openWin 2234-2243, `const r = rect \|\| a.p;`. The fourteen defaults (1546-1559) | n/a | NOT CLAMPED, AND SOME CALL SITES ARE OUTSIDE THE FIELD. wm_o | BACKABLE |
| 21 | TILE — the one command that writes geometry from the grid ra | toast('tiled onto the module', list.length + ' plates on ' + cSpan + ' | tileAll 2738-2758. `cols = Math.min(3, Math.max(1, Math.ceil(Math.sqrt(list.leng | the toast's own ratio r2('--zd-cut','--zd-base') is recomput | MISSING — no tile-all in wm.c that I found. If it is added,  | READ |
| 22 | THE SNAP ZONES — zlOS-ONLY. The prototype has NO edge snappi |  | snap.c:50 `#define SNAP_EDGE 16` — 16 RAW PIXELS of pointer position, not design | n/a — the preview outline is drawn by snap_preview_draw (wm. | PRESENT, AND THE ZONE TEST IS RESERVE-BLIND. Because px <= 1 | BACKABLE |

**Build order.** Build in this order; each step is independently verifiable and the early ones are the cheap ones.

1. RE-ENABLE THE CLAMP (elements 3, 4). Delete `(void)clamp_to_field; /*PLANTED: clamp disabled*/` at kernel/src/graphics/windowing/wm.c:2229 and insert `clamp_to_field(&x, &y);` as the FIRST statement of wm_move() after the `wm_is_open` guard — before the equality early-out at wm.c:2230, or an already-out-of-range window can never be rescued. This is one line and it fixes the drag path, the keyboard path and the snap path at once, because snap_to_rect calls wm_move (wm.c:3909). Do this first: it is the whole of rule 1 for the direction that has no route back.

2. CLAMP wm_open (element 20). Same call, at wm.c:1925. Do it second because it is the same helper and because it makes the prototype's own boot-log line `no reserved strut api, rail clamp applied in wm_open` (presswork-prototype.html:1448) TRUE — right now that is a printed figure the system does not back, which is the exact failure this project's read-never-write rule exists to prevent. Then fix the six raw-coordinate call sites in kernel.zl (5164, 9373, 9384, 9394, 9404, 9419) to use the existing desk_x()/desk_y() helpers (kernel.zl:10586-10587); three of them currently open under the rail.

3. LEAVE A TEST BEHIND, BEFORE going further. There is currently NO assertion anywhere that a dragged window clears the rail — I grepped kernel/tests/host/ for `clamp` and for `clamp_to_field` and found nothing on this path; the on

**Spot-check disagreed:** VERDICT: ok=false. Entries 1 and 2 share one geometry error (30 px short). Entry 3 has one unit error ("design px"). Every line citation, every token, and every quoted code literal in all three entries is byte-exact — the errors are in the derived screen-space arithmetic, not the quotes.  ===================================================================== DEFECT A — entries 1 AND 2: the screen-space field rect is 30 px short ===================================================================== Entry 1 says: "Screen-space field origin = (rail_w, strip_h) = (170, 30) design px; field size = (screen_w - 170) x (screen_h - 30 - 46)." Entry 2 says: "In screen space that is x=rail_w, y=strip_h, w=screen_w-rail_w, h=screen_h-strip_h-foot_h."  Both are wrong for the prototype as it renders. Ther

## rail-register — 26 elements, 0 missing

Drawn today by: kernel/src/kernel.zl — draw_rail_static() :1247 (baked: ground, edge runs, ident, rules, WORKSPACE caption); draw_rail_reg() :1583 (live: REGISTER caption + all rows); draw_rail_row() :1052; draw_rail_win() :1127 (tail rows); rail_rule1/rail_rule2 :1175-1176; rail_sect() :1192; draw_rail_ws() :1293;

| # | element | exact text | geometry | tokens | zlOS today | backing |
|---|---|---|---|---|---|---|
| 1 | #rail — the band itself. Fixed-width flex column, first chil |  | width and flex-basis 170px (--zd-rail-w, calc(170px * var(--ui))); flex: 0 0 170 | ground --zd-raise (NOT --zd-base; the two-material law is no | CORRECT. kernel.zl:1249-1253 fills rail_w() x px_h() with TH | N/A |
| 2 | .ident line 1 — the machine's name. The ONLY .t-big in the w | zlos | Parent .ident: padding 11px 10px 7px (top / --zd-pad left+right / bottom). Line  | ink --zd-text-0 (set inline by #rail .ident .w, overriding . | CORRECT. kernel.zl:1268 ui_disp(pad_d(), 11*u, "zlos", theme | N/A |
| 3 | .ident line 2 — the build line under the name. | 0.3 · x86_64 · presswork | Source markup is `0.3 &middot; x86_64 &middot; presswork` — U+00B7 MIDDLE DOT, o | ink --zd-text-3 (#rail .ident .b). zl: theme(TH_TEXT_DIM)=ZD | PARTIAL — kernel.zl:1269 draws text_aa(pad_d(), 11*u + 24*u, | N/A |
| 4 | .r2 — RULE 2, the heavy rule. Separates ident from the regis |  | height 2px (calc(2px * var(--ui)) — this one DOES scale with ui); flex: none; fu | --zd-lit. 1.56:1 on the rail, but twice the width of rule 1  | CORRECT. rail_rule2() kernel.zl:1176 fills 0,ry,rail_w(),RUL | N/A |
| 5 | .sect — the REGISTER section caption row. Flex row holding t | register | display:flex; align-items:center; gap 6px; padding 7px 10px 4px. Total height 7  | ground is the rail's own --zd-raise (no background set). Lab | CORRECT and slightly richer. rail_sect() kernel.zl:1192-1224 | N/A |
| 6 | #regcount — the running/total fraction, pushed to the right  | 3/14 | span classes: spread t-num ink3. .spread = margin-left:auto (pushes to the right | ink --zd-text-3 (.ink3). Explicitly NOT --zd-text-inert: the | PRESENT, different denominator, and the numerator has a filt | READ |
| 7 | .r1 — RULE 1, the hairline groove. Closes the caption block, |  | height EXACTLY 1px — flex: 0 0 1px, NOT scaled by ui. This is the whole point of | --zd-cut. 1.84:1 on the rail. zl: theme(TH_CUT). | CORRECT. rail_rule1() kernel.zl:1175 advances by exactly 1 p | N/A |
| 8 | #reg — the scrolling register band. The only flexible block  |  | overflow-y: auto; overflow-x: hidden; flex: 1 1 auto; min-height: 0 (the min-hei | no ground of its own — the rail's --zd-raise shows through.  | DIVERGES — zlOS CLIPS, it does not scroll. rail_reg_h() (:10 | N/A |
| 9 | .slot — one register row. The unit of the whole surface. Fou |  | display:flex; align-items:center; gap 7px; height 26px (--zd-row-h); padding 0 6 | idle ground: none set, the rail's --zd-raise shows through.  | CORRECT. draw_rail_row() kernel.zl:1052-1113. Fills the row, | N/A |
| 10 | .slot .mk — THE REGISTER MARK. Overprint job 1 of four, and  |  | position:absolute; left:0; width 3px (--zd-focus-bar, calc(3px * var(--ui))). RU | running --zd-verm (#E8734F dark / #B7311A light). focused -- | CORRECT IN VALUE, NO ANIMATION. kernel.zl:1071-1077: mkh = r | N/A |
| 11 | .slot .n — the row's number. Its address, not decoration. | 01 | First flex child, so its left edge is at x=13. font-family mono; font-size 11px  | idle --zd-text-3; running --zd-text-2 (.slot.run .n); focuse | CORRECT. kernel.zl:1078-1084. num_aa at x = RAIL_SLOT_PL * u | N/A |
| 12 | .slot icon — the row's address in picture form. |  | 14x14 SVG, viewBox 0 0 24 24, from icon(a.ico, 14) at prototype line 2356. fill: | currentColor, inherited from .slot itself — so it is --zd-te | DIVERGES TWICE, both documented in the file. (a) SIZE: 24, n | N/A |
| 13 | .slot .nm — the app's name. Lowercase, and that is a correct | terminal | Sans face (--sans), font-size 13px (--fs-md) — the ONE thing in the row that is  | idle --zd-text-3 (inherited from .slot); running --zd-text-1 | CORRECT, with one recorded size divergence. kernel.zl:1095 l | N/A |
| 14 | .slot .us — THE ROW'S FIGURE. The live per-window number, an | 1119 | margin-left: auto — pushed hard right, its right edge at 170 - 6 = 164px. font-f | ink --zd-text-3 (.slot .us). Same rung as #regcount, and for | PRESENT BUT DRAWING THE WRONG QUANTITY, AND THE COMMENT EXPL | READ |
| 15 | .slot:hover:not(.act) — the hover state. |  | Whole 26px row is the target. No transition declared, so it snaps. | ground --zd-float (#4B4440 dark) — one rung off the plane, t | CORRECT. kernel.zl:1060 `if hovered == 1 { ground = theme(TH | N/A |
| 16 | .slot.act — the focused row's ground. The row drops to the p |  | Whole row. No transition. | ground --zd-base (#322B27) — the PLATE's value, one rung BEL | CORRECT. kernel.zl:1062. There is no ring and no glow on any | N/A |
| 17 | Tail rows — open windows whose app has no fixed register slo |  | Identical row geometry to .slot. Numbering CONTINUES the register's, so a catalo | Same as .slot, except the idle number ink starts at TH_TEXT_ | EXISTS, kernel.zl:1127-1173 draw_rail_win(). The mark is unc | READ |
| 18 | .r2 — RULE 2 again. Closes the register band. |  | 2px, full width, scales with ui. | --zd-lit / theme(TH_LIT). | CORRECT. Drawn by draw_rail_static walking UP from the foot: | N/A |
| 19 | .sect — the WORKSPACE section caption. Same row shape as the | workspace | Identical to element 5: flex, gap 6px, padding 7px 10px 4px, 26px tall. Single . | ground --zd-raise, ink --zd-text-3. | CORRECT and BAKED (it never changes, so it lives in the wall | N/A |
| 20 | #rail .wsrow / .wsb — the workspace buttons. TWO of them in  | ws 01 | .wsrow: display:flex; flex:none. Each .wsb: flex:1 (so each is exactly half of 1 | off: ground is the rail's --zd-raise, label ink --zd-text-3. | WRONG COUNT AND NO LABEL. kernel.zl:634 WS_N = 3, with the c | N/A |
| 21 | .r2 — RULE 2. Closes the workspace row. |  | 2px, full width. | --zd-lit / theme(TH_LIT). | CORRECT. kernel.zl:1279. | N/A |
| 22 | #rail .stat — the machine's own readings block. Three rows. |  | padding 3px 10px. Three .strow children at 19px each. Total 3 + 57 + 3 = 63px (m | ground --zd-raise / TH_PANEL_HI. | CORRECT height and ground. kernel.zl:1391-1420, sy anchored  | N/A |
| 23 | .strow — one reading row: icon, key, dot, state word. Three  | net | height 19px, gap 6px, align-items center. Children left to right: (1) a 13x13 SV | key ink --zd-text-3 (.t-lab). Dot background: ok -> --zd-ok  | THREE ROWS, RIGHT WORDS, NO ICON, ONE INK DIVERGENCE. rail_s | READ |
| 24 | .r1 — RULE 1, the hairline. Separates the stat block from th |  | exactly 1px, never scaled. | --zd-cut / theme(TH_CUT). | CORRECT. kernel.zl:1281. | N/A |
| 25 | #rail .clock .hm — the time. The largest, brightest thing in | 14:32 | Parent .clock: padding 5px 10px 10px. .hm is display:block, font-family MONO, fo | ink --zd-text-0 / theme(TH_TEXT_HI). Ground is the rail's -- | CORRECT AND BETTER GUARDED, one face divergence. draw_rail_c | READ |
| 26 | #clockd — the line under the clock. The prototype hardcodes  | 26 aug 2026 | span.t-num.ink3 with an inline display:block. Mono, 11px (--fs-sm), line-height  | ink --zd-text-3 (.ink3) / theme(TH_TEXT_DIM). | CORRECT AND MEASURED. draw_rail_date() kernel.zl:1466-1479 a | READ |

**Build order.** Build in the order the pixels stack, because every later block is positioned against an earlier one and three of them are already correct in zlOS.

1. FIX THE STALE FACTS FIRST, before drawing anything (they are one-line edits and they change what later steps draw): (a) WS_N 3 -> 2 at kernel.zl:634 and delete the "The reference has three" comment — that count comes from ds-reference.html, the SUPERSEDED predecessor, and the authority prints two; (b) rewrite the app_ws() header comment at kernel.zl:625-640, which cites "ds-reference.html's three tables" as its source; (c) delete the "THIS KERNEL CAN ONLY HONESTLY DRAW THE SECOND" paragraph at kernel.zl:955-984 — wm_win_us() shipped (wm.c:4294) and is bound as native wm_appus (runtime_kernel.c:1734), so the reason the rail draws kpx no longer holds.

2. Element 6 (.us / the row figure) — swap rail_slot_kpx(win) for wm_appus(win) in draw_rail_row's call site (:1111) and draw_rail_win (:1164-1165). This is the single highest-value change on the surface: it is the one live per-app measurement the rail exists to carry, and it went from CANNOT to READ while the comment saying otherwise stayed.

3. Element 25 (the mixed-filter regcount numerator) — rail_running_n() at :1227 counts fixed rows with no workspace filter and tail rows with one. Fix before touching the caption's format, or you will be reading a number that disagrees with the rows under it.

4. Elements 20-21 (workspace buttons) — after step 1a the row is two buttons; add t

**Spot-check disagreed:** ENTRY 1 (#rail): EXACT. No corrections. Verified against presswork-prototype.html:417-420 (`#rail { width: var(--zd-rail-w); flex: 0 0 var(--zd-rail-w); background: var(--zd-raise); display: flex; flex-direction: column; position: relative; z-index: 60; border-right: 1px solid var(--zd-cut); }`), :422-423 (`#rail::before { content: ''; position: absolute; left: 0; top: 0; bottom: 0; width: 1px; background: var(--zd-lit); }`), :165 (`--zd-rail-w: calc(170px * var(--ui));`), :178 (`--zd-r-bolt: 0px;` — and #rail declares no border-radius at all), :982 (`#os { position: fixed; left: 0; right: 0; top: calc(30px * var(--ui)); bottom: 0; display: flex; overflow: hidden; background: var(--zd-void); }`), :984 (`<div id="rail">` is #os's first child). zl citations correct: ui.c:155 `theme.panel_hi 

## strip-foot — 22 elements, 0 missing

Drawn today by: kernel/src/kernel.zl — raster: constants 1778-1802, budget_w() 1834, strip_rd_w() 1837, strip_rd_on() 1854, raster_x0() 1859, raster_x1() 1862, num3() 1869, draw_strip_rd() 1876, raster_cols() 1931, raster_tick() 1947, draw_strip() 1965. Foot: RULER_* constants 2231-2262, mem_top() 2266, foot_y() 22

| # | element | exact text | geometry | tokens | zlOS today | backing |
|---|---|---|---|---|---|---|
| 1 | #raster — the band itself. Full width of the field (rail's r |  | height 30 dp (--zd-strip-h = calc(30px * var(--ui)), proto:166; zlOS STRIP_H_D = | ground --zd-cut / TH_CUT (=ZD_CUT #0A0300, L* 1.17). LIGHT L | kernel.zl:1971 fill_rgb(sx, 0, sw, sh, theme(TH_CUT)). Corre | READ |
| 2 | The strip's bottom rule — the 2 dp struck ZD_LIT run that se |  | 2 dp tall (--zd-rule via border-bottom: calc(2px * var(--ui)), proto:487; zlOS R | --zd-lit / TH_LIT (=ZD_LIT #6F6864, 2.5423:1 on ZD_BASE). Sa | kernel.zl:1972 fill_rgb(sx, sh, sw, RULE_H * u, theme(TH_LIT | N/A |
| 3 | The raster label's own clearing plate — a patch of the strip |  | x from band left + 1, y = 1, width = ui_capsw("RASTER", T_CAPTION) + 2 * pad_d() | --zd-cut / TH_CUT — the band's own ground, repainted. Light: | kernel.zl:2035-2036, exactly this. Correct. Prototype achiev | N/A |
| 4 | The raster label. Left-hand legend of the band. Vertically c | RASTER — uppercase as RENDERED. The prototype's markup says lowercase  | x = band left + pad_d() = +10 dp. y = (strip_h() - label_h(T_CAPTION)) / 2 — cen | ink --zd-text-2 / TH_TEXT_2 (=ZD_TEXT_2 #C8C2B9). NOT the de | kernel.zl:2037 ui_caps(sx + pad_d(), (sh - label_h(T_CAPTION | N/A |
| 5 | The base rule of the plot — a 1 px horizontal line the colum |  | y = strip_h() - RASTER_FOOT * ui(). Spans x0..x1 only (the plot's own gutters),  | --zd-text-inert / TH_SURF_7 (=ZD_TEXT_INERT #6E685F). STRUCT | kernel.zl:1990 fill_rgb(x0, base, x1 - x0, 1, theme(TH_SURF_ | N/A |
| 6 | THE COLUMN LOOP. One column per COMPOSITE — never per vblank |  | PITCH 3 dp per column: 1 dp of bar, 2 dp of gap (RASTER_PITCH = 3, kernel.zl:177 | normal column --zd-steel / TH_STEEL (=ZD_STEEL #7FB2E0). Ove | kernel.zl:1994-2019, complete: raster_cols() 1931, ring read | READ |
| 7 | THE READOUT (.rd) — six fields at the right-hand end of the  | Six children in this exact left-to-right order: ADVANCE \| composite \ | Right-anchored: block right edge at px_w() - pad_d(). GAP between every adjacent | labels: prototype dark = .t-lab default --zd-text-3; prototy | kernel.zl:1876-1929 draw_strip_rd(), walking the identical f | N/A |
| 8 | Readout field 1 — the ADVANCE label and its counter. Answers | ADVANCE followed by the count followed by composite. THE KERNEL PRINTS | Label at the block's left edge, then +capsw(ADVANCE) + 11 dp gap. The number is  | label TH_TEXT_2 (see element 7). Value TH_TEXT (=ZD_TEXT_1 # | kernel.zl:1890-1896. Complete. | READ |
| 9 | Readout field 2 — IDLE WAKEUPS/H and its figure. Answers wha | IDLE WAKEUPS/H then 0. Exactly one cell wide for the value. The protot | capsw(IDLE WAKEUPS/H) + 11 dp gap + 1 cell + 11 dp gap. | label TH_TEXT_2, value TH_TEXT (ZD_TEXT_1). If a frame-clock | kernel.zl:1909-1920. Complete, with the honesty note attache | CANNOT |
| 10 | Readout field 3 — BUDGET and the presentation deadline. Answ | BUDGET then 16 667 us — thousands digit(s), a printer's space (plain A | capsw(BUDGET) + 11 dp gap + budget_w(). budget_w() = s3_numw(idiv(wm_budget(), 1 | label TH_TEXT_2. Value TH_TEXT_DIM (=ZD_TEXT_3 #B9B3AA) — th | kernel.zl:1921-1928. Complete. | READ |
| 11 | The kernel's 800x600 fallback path for the whole readout — n |  | When strip_rd_on() == 0: raster_x1() = px_w() - pad_d() (kernel.zl:1863) instead | — | kernel.zl:1854-1857 and 1862-1866 and 2038-2040. Complete. T | READ |
| 12 | #foot — the band. Two instruments in one band, which is the  |  | height 46 dp (--zd-foot-h, proto:167; FOOT_H_D = 46, kernel.zl:390, via foot_h() | ground --zd-raise / TH_PANEL_HI (=ZD_RAISE #413A36, ui.c:155 | kernel.zl:2346-2360 draw_foot(). Complete: rule 2352, ground | READ |
| 13 | The memory ruler's own band clear. Repainted independently o |  | fill from (fx+1, ry - 3*ui) sized (fw-2) x (foot_h() - band_h() - 3*ui). ry = fo | --zd-raise / TH_PANEL_HI — the foot's own ground, repainted. | kernel.zl:2380. Present. The reason it clears first is state | N/A |
| 14 | The MEMORY label — left legend of the ruler row. | MEMORY — uppercase as rendered (markup at proto:1031 is lowercase 'mem | x = foot left + pad_d() (10 dp). y = ry + 2*ui where ry = foot_y() + 3*ui. Follo | --zd-text-3 / TH_TEXT_DIM (=ZD_TEXT_3 #B9B3AA) — .t-lab's de | kernel.zl:2381. Correct rung. | N/A |
| 15 | #rulerbar — the well the regions are packed into. A sunken t |  | height 11 dp INCLUDING the ring (proto:565 height: calc(11px * var(--ui)) under  | well fill --zd-well / TH_SURF_WELL (=ZD_WELL #17100C). Ring  | kernel.zl:2391-2401. Complete, including the four separate 1 | N/A |
| 16 | THE REGION WALK — the packed segments. THE BAR IS PACKED BY  |  | Each region's width = idiv(idiv(bytes, 1024) * iw, idiv(total, 1024)) — THE DIVI | THREE INKS AND EACH MEANS SOMETHING (kernel.zl:2405-2413). - | kernel.zl:2415-2421 and 2424, with ruler_seg() at 2317-2335. | READ |
| 17 | The segment separators — the rule between adjacent regions. |  | A 1 px vertical line at the LAST pixel of each segment, i.e. drawn INSIDE the re | --zd-cut / TH_CUT. | kernel.zl:2331 fill_rgb(rx + sw - 1, ry, 1, rh, theme(TH_CUT | N/A |
| 18 | THE DATUM MARK. Overprint job 4 of 4 — one vermilion hairlin |  | 1 px wide, at the x where the framebuffer region STARTS (captured before ruler_s | --zd-verm / TH_ACCENT (=ZD_VERM #E8734F). The overprint. Its | kernel.zl:2428-2430: dx captured before the fb seg at 2427,  | READ |
| 19 | The free tail — the last region, drawn as the bar's REMAINDE |  | fill from the walk's final x to xe. This is the correction for accumulated round | --zd-well / TH_SURF_WELL — the same value as the empty track | kernel.zl:2429. Complete. | READ |
| 20 | THE RULER READOUT — right-aligned, fixed-width, on fixed mon | Column-addressed, 60 cells at full measure: col 0 '0x' + 8 hex digits  | THREE MEASURES, dropping widest-first (ruler_rd_cells(), kernel.zl:2467-2472): R | Addresses and figures --zd-steel / TH_STEEL — they are readi | kernel.zl:2432-2461. Complete, including hexat() at 2481-249 | READ |
| 21 | The slug's separating rule and band — a 1 px groove above th |  | Rule at y = sy - 2*ui where sy = foot_y() + foot_h() - band_h(); x from foot lef | --zd-cut / TH_CUT. | kernel.zl:2356-2358. Correct. | N/A |
| 22 | THE PRINTER'S SLUG — one mono line stating the machine and t | THE PROTOTYPE'S LINE, verbatim from proto:2509-2514, separator ' · ' ( | Starts at foot left + pad_d(), baseline sy + 2*ui. Mono, --fs-sm 11 dp, no track | --zd-text-3 / TH_TEXT_DIM (=ZD_TEXT_3) for the entire line,  | kernel.zl:2551-2640 draw_slug(). Complete and shipping. Re-l | BACKABLE |

**Build order.** Build strictly bottom-up, because each stage is verifiable on screen before the next has anything to stand on, and because three of the four stages are already shipped in kernel.zl and only need the deltas applied.

1. GROUNDS AND RULES FIRST (elements 1, 2, 12, 13). Two fills and two 2 dp ZD_LIT rules. This is the only stage that can be wrong in a way nothing else reveals: get strip_h()=30, foot_h()=46, RULE_H=2 and the rule ON THE OUTSIDE of each band (strip's rule below it, foot's rule above it) before anything is drawn into them. Already correct at kernel.zl:1971-1972 and 2352-2353.

2. THE RASTER LABEL PLATE AND BASE RULE (elements 3, 4, 5). The label plate must be painted BEFORE the columns in draw order but its x-extent must be known before the plot's x0 can be computed — that circularity is why raster_x0() (1859) is a function and not a constant. Landing the base rule here means a plot with zero samples still reads as an instrument rather than as an empty black band.

3. THE COLUMN LOOP (element 6) and only then THE READOUT (elements 7-12). The readout is what bounds the plot's right edge, so it looks like it should come first — it must not. strip_rd_on() (1854) can DROP the readout entirely at 800x600, and if you build the readout first you will size the plot around a thing that sometimes is not there. Build the plot full-width, then subtract.

4. THE RULER (elements 14-20), then THE SLUG (elements 21-22). The ruler is the harder half and the one with a live failure 

**Spot-check disagreed:** VERDICT: entry 1 EXACT (with one prose fix). Entry 2 WRONG. Entry 3 WRONG — it is not in the prototype at all.  Root cause of both failures: the other agent transcribed kernel.zl's existing `draw_strip()` (kernel/src/kernel.zl:1965-2027) and presented it as a derivation from the prototype. Every number in entries 2 and 3 matches the kernel exactly and the prototype not at all. The prototype is the stated authority, so where they disagree the kernel is what needs changing.  ═══ ENTRY 1 — #raster band: EXACT on every stated value ═══  Verified verbatim, /home/roy/Documents/repos/zl-linux-presswork/docs/design/presswork-prototype.html:486-488:   #raster { height: var(--zd-strip-h); flex: 0 0 var(--zd-strip-h); background: var(--zd-cut);             border-bottom: calc(2px * var(--ui)) solid v

## settings-controls — 13 elements, 10 missing

Drawn today by: kernel/src/kernel.zl:8890 `fn set_tab_body(x,y,w)` dispatches to set_ladder_body:8563 / set_focus_body:8231 / set_bound_body:8687 / set_press_body:8770. ONLY set_focus_body has controls — kernel.zl:8165 `fn set_focus_ctl(x,y,w)` draws the knockout toggle (8170) and the focus-bar slider (8182). The o

| # | element | exact text | geometry | tokens | zlOS today | backing |
|---|---|---|---|---|---|---|
| 1 | SURFACE LADDER — segmented, 2 options. Tab 0 (LADDER), immed | label: "surface ladder"  \|  options verbatim, lowercase, in this orde | Row: .row, height 26 dp, display flex, align-items center, gap 8 dp, flex none.  | seg outer border --zd-cut; option ground --zd-raise; option  | MISSING. kernel.zl:8563 set_ladder_body draws the prose and  | BACKABLE |
| 2 | UI SCALE — slider, 80..140, step 1. Tab 0 (LADDER), second a | label: "ui scale"  \|  lo 80, hi 140  \|  unit string " %" WITH A LEAD | Row: .row, height 26 dp, gap 8 dp. Child 1 label: .t-md .ink1, width 146 dp, fle | track ground --zd-well, track ring --zd-cut, FILL --zd-steel | MISSING as a control, but the mechanism is complete. kernel. | READ |
| 3 | THE KNOCKOUT — toggle. Tab 1 (FOCUS), first control. NOT in  | label: "the knockout"  \|  note: "off falls back to graphite's wash in | Row .row height 26 dp, gap 8 dp. Switch .sw2: 34 x 17 dp, flex none, border 1px, | off: ground --zd-well, ring --zd-cut, knob --zd-text-inert ( | SHIPS. kernel.zl:8170 draws it, :8175 draws the label verbat | READ |
| 4 | FOCUS BAR — slider, 1..6. Tab 1 (FOCUS), second control. Als | label: "focus bar"  \|  lo 1, hi 6  \|  unit " dp" with a leading spac | Identical to the ui scale row: 146 dp label, 11 dp .mtrack, 62 dp right-flushed  | Same as ui scale. | SHIPS. kernel.zl:8182 set_slider(...ui_fbar_dp(), 1, 6); hit | READ |
| 5 | OCCLUSION EDGE — toggle. Tab 2 (BOUNDARY), the only control  | label: "occlusion edge"  \|  note is COMPUTED, not literal: "off falls | Toggle row, identical to element 3: 26 dp row, 8 dp gap, 34x17 switch with 13 kn | Same as element 3. | MISSING. kernel.zl:8687 set_bound_body draws the ceiling pro | BACKABLE |
| 6 | LABEL TRACKING — toggle. Tab 3 (PRESS), first control, direc | label: "label tracking"  \|  note: "off is what shipping without UI_F_ | Toggle row, identical to element 3. | Same as element 3. What the toggle DRIVES: .t-lab and .t-big | MISSING as a control — and BOTH the prototype's note and ker | BACKABLE |
| 7 | DESK MODULE GRID — toggle. Tab 3 (PRESS), second control. | label: "desk module grid"  \|  note is COMPUTED: S.cols + ' x ' + S.ro | Toggle row, identical to element 3. What it DRAWS (drawGrid, :2408-2427): margin | Grid rules: --zd-grid (zl role TH_GRID = 38, kernel.zl:109). | MISSING entirely. `ui_grid(const char *tracks)` (ui.h:700, n | BACKABLE |
| 8 | CROP MARKS — toggle. Tab 3 (PRESS), third control. | label: "crop marks"  \|  note: "overprint job 3, vermilion, at the mar | Toggle row, identical to element 3. What it DRAWS (:2428-2436), verbatim geometr | Marks: --zd-verm (zl role TH_ACCENT = 5, kernel.zl:60). Togg | MISSING. Nothing in the tree draws crop marks. | N/A |
| 9 | MEMORY MAP RULER — toggle. Tab 3 (PRESS), fourth control. | label: "memory map ruler"  \|  note: "Datum, on the foot"  \|  no toas | Toggle row, identical to element 3. What it CONTROLS: body.noruler hides #rulerr | Ruler ground --zd-well, ring and region separators --zd-cut, | MISSING. No foot bar, no ruler, no memory-map enumerator any | CANNOT |
| 10 | PRINTER'S SLUG — toggle. Tab 3 (PRESS), fifth control. | label: "printer's slug" (note the ASCII apostrophe)  \|  note: "PLATE, | Toggle row, identical to element 3. What it CONTROLS: body.noslug hides #slugrow | Slug ink --zd-text-3; #slugrow top rule --zd-cut; foot groun | MISSING. No foot bar exists. kernel.zl:430 `fn foot_h()` rea | BACKABLE |
| 11 | MOTION — toggle. Tab 3 (PRESS), sixth and last toggle. | label: "motion"  \|  note: "RISE 90 / TRAVEL 160 / SETTLE 240, one cur | Toggle row, identical to element 3. What it CONTROLS: body.nomo sets `* { transi | Toggle row tokens as element 3. The numbers in the note are  | MISSING as a control. The SINK EXISTS AND IS ORPHANED FROM z | BACKABLE |
| 12 | RASTER CLOCK — segmented, 2 options. Tab 3 (PRESS), first se | label: "raster clock"  \|  options verbatim: "composite", "frame"  \|  | Seg row, identical to element 1: 26 dp row, 146 dp label column, 8 dp gap, .seg  | Seg tokens as element 1. In the strip readout the frame-mode | PARTIAL — 'composite' is hardwired and is the only mode. ker | READ |
| 13 | PER WINDOW TIMING — segmented, 3 options. Tab 3 (PRESS), sec | label: "per window timing"  \|  options verbatim, in this order: "meas | Seg row, identical to element 1, with three options instead of two. | Seg tokens as element 1. Off mode sets body.nous, which hide | MISSING as a control — and the prototype's justification for | READ |

**Build order.** Ranked cheapest-to-ship. The rank is (state bit) + (does the sink exist) + (are the printed figures already READ) — not by how much of the pane they light up.

1. UI SCALE SLIDER (element 2). Zero new C. ui_theme_q8 and ui_metric(UM_SCALE_Q8) are both registered; kernel.zl's set_slider/set_slider_v draw and hit-test it already. Range verified inside ui_theme_init_q8's 192..768 clamp. The only care needed is the apply funnel — ui_theme_init wipes knock/fbar/accent, documented at settings.c:154-161.

2. PER WINDOW TIMING SEG (element 13), measured + off only. wm_appus is already registered and already measures per window. Defer the 'repaint' option until wm.c gets a wm_repaint_px accessor. Ship this early precisely because it kills a stale claim: kernel.zl:8790 currently tells the reader this cannot be done.

3. MOTION TOGGLE (element 11). One native line over wm_set_anim (wm.c:1299), a sink that already exists and is already exercised by wmtest.c:1157/1171.

4. OCCLUSION EDGE TOGGLE (element 5). One static + one branch at wm.c:2622 + a native pair. The fallback the note promises (ZD_CUT) is literally what theme.border already is (ui.c:159), so nothing has to be invented.

5. LABEL TRACKING TOGGLE (element 6). One static inside uikit.c track_dev() (:377) + a native pair. Zero clip risk because measure and draw share text_run (:395). Rewrite the note and kernel.zl:8790-8793 in the same commit — both currently assert the feature does not exist.

6. CROP MARKS TOGGLE (element 8). 

**Spot-check disagreed:** ONE HARD ERROR (entry 1), plus four spec-accuracy defects that would mislead a builder. Everything else re-derives exactly.  ═══ THE ERROR: entry 1, raking toast body, "edge skew jumps to 2.05%" — WRONG.  Prototype line 2803-2809 (presswork-prototype.html):     if (p[0] === 'ladder') toast('ladder: ' + p[1],       p[1] === 'raking'         ? 'graphite\'s six rungs, presswork\'s knockout on top. smallest step ' +           r4('--zd-raise', '--zd-float') + ':1, and the edge skew jumps to ' +           edgeSkew().toFixed(2) + '% - the lit run stops reading at one loudness'         : 'widened. smallest step ' + r4('--zd-raise', '--zd-float') + ':1, edge skew ' +           edgeSkew().toFixed(2) + '%, ZD_KO_EDGE resolves to ZD_LIT\'s own value');  edgeSkew() is defined at :1137-1140:     functio

