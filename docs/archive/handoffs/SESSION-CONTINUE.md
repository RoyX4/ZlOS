# Continuing from the merge session — 2026-08-19

Hand-off for a fresh chat that picks up where the eleven-track merge session
left off. Operational state, not project state — for *what is open and who is
doing it*, this is the file; for *what the project needs*, read
`docs/evidence/status-audits/STATE-OF-THE-PROJECT-2026-08-19.md`.

## Read these three, in this order

1. **`docs/evidence/status-audits/STATE-OF-THE-PROJECT-2026-08-19.md`** — 57 open items, each with the command that
   established it. The one page that says where the project is.
2. **`docs/WORKING-RULE.md`** — everything lands on `main`, the same day. Why,
   with the numbers, and the one thing that is actually forbidden.
3. **`docs/evidence/MERGE-EVIDENCE.md`** — what the eleven-track merge cost and the
   failure classes it kept hitting. Read §2 before assuming a merge artefact is
   impossible.

## Where things stand

`main` — everything is on it. All sixteen branches merged; nothing is waiting to
be integrated. One folder: `~/Documents/repos/zl-linux`.

Gate is green end to end: builds, 27 host harnesses, both static checkers, and
every boot path — BIOS, UEFI, our own bootloader, the zlfs power-cycle, the
clock. `gates/land-gate.sh`, backgrounded, on a quiet box.

There is a pre-push hook now. It boots QEMU and runs five gates before letting
anything reach GitHub, and it has already blocked two of this session's pushes —
correctly, both times.

## What is in flight

- **A second worktree, `zl-browser2`**, on `desktop/browser-next`. A session is
  writing a CSS engine there. It merges into `main` when they land it; do not
  delete that directory.
- **`kernel/docs/archive/prompts/LOOK-AND-SPEED-PROMPT.md`** — written, not started. The next
  piece of work.
- Several sessions leave uncommitted files in this checkout. `git status` before
  you commit, stage explicit paths, never `git commit -a`. Two files —
  `TODO.md` and `docs/JOURNAL.md` — are regenerated by the post-commit hook on
  purpose and ride along with the *next* commit.

## What is blocked on Zac, and cannot be unblocked any other way

1. **Does the mouse feel fixed?** The pointer fix landed (`fix/pointer-drain`:
   one owner for the xHCI event ring, which two things were draining at once).
   Its own tests go 14-fail-before to 26-pass-after. No gate can answer whether
   it *feels* right. `./try.sh`, move the mouse, drag a window by its title bar,
   drop one on the left edge.
2. **The look decision.** `docs/design/zlOS-design-northstar.html` says "alpha
   blends but no gaussian blur", "vertical gradients only", "small radii". The
   kernel has a cached gaussian blur, radial *and* conic gradients, and radius
   raised 5 → 12. All three shipped as v10 items and gated green, so they are
   decisions against a different reading of the reference, not bugs. Someone has
   to say which wins. See `LOOK-AND-SPEED-PROMPT.md` Part 1.
3. **What specifically looks worse than v10?** Both screenshots are committed
   under `docs/shots/`. The rendering code is byte-identical to the branch that
   drew the good one, so it is a parameter, not deleted code — but nobody has
   named which parameter. A human pointing at the picture and saying "that" is
   worth more than another sweep.

## Closed since this file was written — 2026-08-19

`docs/GUARDS-THAT-DID-NOT-GUARD.md` is the evidence for all of it. Read that
before the list below; three of these were guards reporting green while
checking nothing, which is this project's recurring failure and now has a page.

- **The EFI `-Werror=` guard was inert.** Settled by measurement, not argument:
  `-w` suppresses those four flags *regardless of position*, so the CI hazard
  scan was right and `CLAUDE.md` was wrong. Behind it sat **33 casts of the
  exact class it names** — including `smp.c`'s `ENTRY_PTR` store, which is the
  documented bug verbatim. `-w` is gone, all 33 are fixed, all four targets
  build clean, and `kernel/wguard.sh` proves the guard bites (and that `-w`
  still silences it) in two seconds with no QEMU.
- **`intel.c`'s `edid_buf`** is out of `HI_BLUR` — as a `static u8[128]`, not a
  new map entry. It never needed a physical address: nothing DMAs into it.
- **Three more `(uptr)hi << 32` sites** (`xhci.c`, `virtio_gpu.c`, `i2c_hid.c`)
  now use the `<< 16 << 16` idiom `nvme.c` and `virtio_net.c` had already
  settled on. That shift is UB on the 32-bit build and gcc warned on every run.
- **The dock digit debris** is fixed in the working tree by another session
  (`kernel.zl`, `tray_x()`-derived damage rect) — uncommitted at the time of
  writing, not mine to land.

## Still open, and one correction to how it is described

**The SMP band-rendering item below points at the wrong file.** Something *does*
call `fb_par_hook` — `smp.c:265`, and the wiring under it is complete. The gap
is one rung up: `smp_go()` appears exactly once in `kernel.zl`, under `cmd == 42`
(the `*` key of the old text shell), so `smp_start()` never runs on a desktop
boot. Follow the instruction as written and you find `fb_par_hook` properly
called, conclude the path is live, and stop. Also worth knowing before you
switch it on: **only one of the seven boot gates runs more than one core.**
`verify-efi.sh` boots `-smp 2`; the other six pass no `-smp` at all and get
QEMU's default of one. (This file first said "no gate covers more than one
core", which was wrong - checked with `grep -o '\-smp [0-9]*' kernel/verify*.sh`.)

## Open, diagnosed, not fixed

- **The dock readout leaves digit debris** (`frame 0 us peak 0 )08 up 1`). Fixed
  x-offsets, no background clear, so a shrinking number leaves its tail.
  `POINTER-PROMPT.md` §1b. Small, visible every boot.
- **`key()`** is called in `kernel.zl` on the Intel panel-handover path and
  defined nowhere, on every branch since `b55f3f9` — the machine halts between a
  successful `panel_up()` and `panel_console()`. Needs someone who knows which
  builtin was meant (`in_key`, `term_key`, `usb_key`).
- **`intel.c`'s `edid_buf`** is hardcoded at `0x0C980000`, inside `HI_BLUR`.
  `intel_set_edid_buffer()` exists; the fix is one call.
- **SMP band rendering is built and probably off.** `fb.c` has `fb_par_hook` /
  `fb_par_run`, and `par_dispatch` is NULL by default — every band runs on one
  core while three sit parked. Possibly one call. Largest untapped speed win.
- **49 files exist only in `refs/wip/*` snapshots**, on no branch — the whole
  `learn/` course, `crypto.c` (543 lines, real, with a test against eight cited
  standards), 7 probes. `git fetch origin 'refs/wip/*:refs/wip/*'` to see them.
- **The EFI `-Werror=` guard may be inert.** `CLAUDE.md` says the four flags work
  because clang applies flags left to right; the CI hazard scan says `-w` is a
  blanket suppression a later `-Werror=` does not survive. One of them is wrong,
  and that guard is what stops the LLP64 pointer-truncation class returning.

## What this session got wrong, because the pattern matters more than the list

Four defects in my own work, every one found by someone checking the **tree**
rather than my write-up:

- `MERGE-EVIDENCE.md` recorded an address fix that had only ever been written
  into a plan. The document was verified against the intention, not the code.
- `mkdisk.sh` lost its kernel-size guard when I took another branch's script
  wholesale. Deleting a check is worse than deleting code.
- `gates/land-gate.sh` built ~26 harnesses and ran none of them. I ran them by
  hand each time, which is the habit a gate exists to remove.
- `check-memmap.sh` iterated a hardcoded nine-name list and could not see the
  constant that actually collided. I then added it to the gate, where it read as
  coverage.

All four fixed; the last validated by breaking the code on purpose to watch the
detector go red. **A thing is done when a command says so and you read the
output.** That is the whole lesson, and it is in `WORKING-RULE.md` too.
