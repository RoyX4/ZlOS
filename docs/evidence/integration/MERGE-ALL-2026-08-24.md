# Complete zl-linux branch integration — 2026-08-24

This document is the durable receipt for merging every remote and local
zl-linux work branch visible on 2026-08-24. It does not cover, modify, or
authorize a merge of the separate `RoyX4/zl` repository.

## Integration boundary

The integration branch started at `origin/main` and contains every remote
branch returned by `git branch -r`. The only branch that was not already an
ancestor after the first eight preservation merges was
`origin/cursor/remaining-work-map-b8fe`; it was merged as the ninth explicit
integration input. A final `git branch -r --no-merged HEAD` returned no branch.

Explicit preservation inputs:

- `origin/codex/audit-trail`
- `origin/codex/local-zlos-snapshot-2026-08-24`
- `origin/codex/performance-architecture`
- `origin/codex/product-wave1`
- `origin/codex/master-program-foundation`
- `origin/files-app`
- `origin/ci/gates-and-agent-brief`
- `origin/secret/desktop-foundry`
- `origin/cursor/remaining-work-map-b8fe`

All other remote topic branches were already ancestors of one or more of
those inputs or `origin/main`; they are therefore present without synthetic
no-op merge commits.

### Post-push local closure audit

The follow-up audit checked more than GitHub:

- every local branch head across the eight named `zl-linux*` repositories;
- all seven worktrees registered by the canonical zl-linux repository,
  including three detached Claude worktrees;
- tracked and untracked status in every registered worktree and named
  standalone clone;
- every remote topic branch against the pushed `origin/main`.

That audit found one genuinely unmerged ref:
`codex/product-wave1` at `c348c2e`. It was a divergent two-commit local
Product Wave snapshot based on the old main, distinct from the already merged
remote Product Wave branch. It was merged as an explicit parent. All of its
file content was already present or superseded in the newer integrated tree,
so the ancestry merge intentionally had no tree delta.

After that merge:

- `git branch -r --no-merged HEAD` returned no branch;
- every discovered local branch head is an ancestor of `HEAD`;
- every registered worktree head is an ancestor of `HEAD`;
- every audited registered worktree and standalone clone had a clean status.

The worktree directories and topic refs were not deleted. “Complete” here
means no discovered unique commit or uncommitted file remains outside the
integration history, not that historical branch names were erased.

## Conflict policy

Conflicts were resolved against the newer physical-audit/runtime baseline,
then additive work was restored explicitly. The important combined seams are:

- deterministic UEFI USB construction plus the bounded raw ZLLOG partition;
- retained compositor surfaces plus app/window lifecycle receipts;
- Ring-3 fault isolation plus the persistent crash recorder;
- current zlfs durability and audit operations plus disk-seeded `hello.zl`;
- current UI/app registry plus the freestanding kernel interpreter;
- current physical EDID allocation plus VBT panel timing and low-vswing policy;
- the complete landing gate plus pointer, network, memory-map and interpreter
  checks.

## Verification

Direct integration checks completed before the complete gate:

- every remote branch is an ancestor of the integration head;
- conflict-marker scan: zero markers;
- shell/Python syntax checks: pass;
- 32-bit kernel: linked, zero undefined symbols, multiboot header present;
- 64-bit kernel: linked, zero undefined symbols, ELF64;
- native UEFI: `BOOTX64.EFI` and `ZLOS.EFI` linked;
- kernel interpreter host route: `run hello.zl` printed `42` and reached
  `EX_RAN`;
- separate ZL language repository suite: out of scope and not pushed.

The contained landing gate was stopped at the user's explicit request to push
immediately. It passed every stage through: prerequisites, authority closure,
dependency/license inventories, toolchain, build identity/graph/snapshot,
32-bit, 64-bit, native UEFI, ELF permissions, and SOURCES recovery selftest.
The remaining SOURCES coverage, host-test inventory/execution, QEMU boots and
joined evidence refresh were not completed in that run and are not claimed.

## Evidence ceiling

Host and QEMU checks cannot promote physical ThinkPad evidence. Panel, fan,
RTC, real USB power-cut, touchpad feel, I219/tethering and NVMe behaviour remain
physical checks even when their code and emulated gates pass.
