# Known-stale doc references, one per line, matched as a fixed substring.
#
# Keep this file as the explicit baseline mechanism. It is currently empty:
# every old exception was either corrected to a real path or rewritten as an
# honest proposed name. Never add a line merely to make a run green.
